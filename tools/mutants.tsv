# One mutant per line: file <TAB> sed expression <TAB> suite that must notice <TAB> label.
#
# Run with tools/mutate_check.sh --all. Every expression must match EXACTLY ONE line; a
# pattern that stops matching (a rename, or a long line re-wrapped) is a hard failure, not
# a skip, because a mutant that silently stops applying reports a coverage it is no longer
# providing.
#
# The sed delimiter is | so no pattern may contain one. A literal & in the REPLACEMENT
# means "the whole match" to sed and has to be written \&.
#
# Patterns are EXTENDED regexes, so + is a quantifier: `_index + delta` matches "_index"
# followed by two-or-more spaces and never the code you meant. Escape it as \+. This one
# fails as PATTERN-NOT-FOUND rather than as a wrong match, so it is loud - but it costs a
# round trip, and every other metacharacter in ( ) [ ] . ? * has the same trap.
#
# The rule: every gate ships with a mutant proving it bites. A rule with no mutant is a
# rule nobody has proven is tested.
scripts/util/lint_core.gd	s|hits.append\(_hit\(path, line_no, RULE_DIRECTION,|if false: hits.append(_hit(path, line_no, RULE_DIRECTION,|	tests/unit/test_lint_core.gd	a raw direction string stops being reported
scripts/util/lint_core.gd	s|return at > 0 and code\[at - 1\] == "."|return false|	tests/unit/test_lint_core.gd	a seeded rng method call is reported as a global one
scripts/util/dir.gd	s|if absf\(v.x\) >= absf\(v.y\):|if absf(v.x) > absf(v.y):|	tests/unit/test_dir.gd	a diagonal tie stops facing sideways
scripts/util/dir.gd	s|const ALL: Array\[D\] = \[D.DOWN, D.LEFT, D.RIGHT, D.UP\]|const ALL: Array[D] = [D.DOWN, D.RIGHT, D.LEFT, D.UP]|	tests/unit/test_dir.gd	the canonical row order silently swaps left and right
scripts/util/json_file.gd	s|if not \(parsed is Dictionary\):|if false:|	tests/unit/test_json_file.gd	a JSON array is accepted where an object is required

# --- M1: the sprite generator's consistency rules ---
scripts/spritegen/sprite_compositor.gd	s|mirrored.flip_x\(\)|pass|	tests/unit/test_gates_consistency.gd	facing left stops being facing right mirrored
scripts/spritegen/sprite_compositor.gd	s|var dy: int = at.y \+ \(bob if rig.bobs\(part_id\) else 0\)|var dy: int = at.y + bob|	tests/unit/test_gates_consistency.gd	the walk bob lifts the feet off the ground line
scripts/spritegen/sprite_compositor.gd	s|if style.outline_mode != SpriteStyle.Outline.NONE:|if false:|	tests/unit/test_gates_consistency.gd	the outline pass stops running
scripts/data/sprite_style.gd	s|return Color8\(_tint_byte\(c.r\), _tint_byte\(c.g\), _tint_byte\(c.b\), 255\)|return c.darkened(0.35)|	tests/unit/test_gates_consistency.gd	a tinted outline is computed in floats and lands off-palette
scripts/spritegen/sheet_builder.gd	s|meta.directions = Dir.ALL.duplicate\(\)|meta.directions = [Dir.D.DOWN, Dir.D.RIGHT, Dir.D.LEFT, Dir.D.UP]|	tests/unit/test_sprite_contract.gd	sheet rows silently swap left and right
scripts/spritegen/sheet_builder.gd	s|meta.anchor = Vector2i\(cell.x / 2, ground\)|meta.anchor = Vector2i(cell.x / 2, cell.y - 2)|	tests/unit/test_sprite_contract.gd	the anchor stops being measured from the pixels
scripts/spritegen/sprite_frames_factory.gd	s|frames.remove_animation\(&\"default\"\)|pass|	tests/unit/test_sprite_contract.gd	the placeholder default animation is left in place
scripts/spritegen/sheet_meta.gd	s|if texture_size != expected:|if false:|	tests/unit/test_sprite_contract.gd	a sheet whose metadata disagrees with its image is accepted
scripts/spritegen/rig.gd	s|if row.length\(\) != width:|if false:|	tests/unit/test_rig_and_style.gd	a ragged part row stops being reported
scripts/data/character_spec.gd	s|chosen_ramps\[slot\] = chosen_ramps\[source\]|pass|	tests/unit/test_rig_and_style.gd	hands and sleeves stop matching skin and cloth
scripts/data/character_spec.gd	s|part_id = str\(rng.derive\("part:" \+ slot\).pick\(options, ""\)\)|part_id = str(rng.derive("part").pick(options, ""))|	tests/unit/test_determinism.gd	every slot draws from one shared stream
scripts/spritegen/tile_set_factory.gd	s|if not bool\(e.get\(\"solid\", false\)\):|if true:|	tests/unit/test_tiles.gd	solid tiles stop blocking movement

# --- M2: the scene layer that consumes the contract ---
scripts/world/sprite_view.gd	s|_sprite.centered = false|_sprite.centered = true|	tests/integration/test_sprite_view_scene.gd	the sprite origin stops being the character's feet
scripts/world/sprite_view.gd	s|_sprite.offset = -Vector2\(meta.anchor\)|_sprite.offset = Vector2.ZERO|	tests/integration/test_sprite_view_scene.gd	the measured anchor stops positioning the sprite
scripts/world/sprite_view.gd	s|if clip_name == _clip and dir == _facing:|if false:|	tests/integration/test_sprite_view_scene.gd	an unchanged pose is re-issued every frame
scripts/ui/sprite_lab.gd	s|if not _gate.accept\(event\):|if false:|	tests/integration/test_sprite_view_scene.gd	one input event is acted on twice

# --- M3: movement, maps and game flow ---
scripts/world/locomotion.gd	s|move = move.normalized\(\)|pass|	tests/unit/test_locomotion.gd	walking diagonally is 41% faster than walking straight
scripts/world/locomotion.gd	s|if absf\(move.x\) >= absf\(move.y\):|if absf(move.x) > absf(move.y):|	tests/unit/test_locomotion.gd	a four-way tie stops going sideways
scripts/world/locomotion.gd	s|var moving := velocity.length\(\) > config.idle_speed_epsilon|var moving := velocity.length() > 0.0|	tests/unit/test_locomotion.gd	drift below the idle threshold keeps the walk animation twitching
scripts/autoload/router.gd	s|return _state == State.WORLD|return true|	tests/unit/test_router.gd	the player can move while a dialog is open
scripts/autoload/router.gd	s|set_state\(_stack.pop_back\(\)\)|set_state(State.WORLD)|	tests/unit/test_router.gd	closing an overlay forgets what it covered
scripts/autoload/router.gd	s|_stack.clear\(\)|pass|	tests/unit/test_router.gd	reset leaves the overlay stack behind
scripts/world/map_data.gd	s|if warp_tiles.has\(at\):|if false:|	tests/unit/test_map_data.gd	a declared exit is reported as a hole in the wall
scripts/world/map_data.gd	s|return Vector2i\(floori\(at.x / tile_size\), floori\(at.y / tile_size\)\)|return Vector2i(int(at.x / tile_size), int(at.y / tile_size))|	tests/unit/test_map_data.gd	a negative coordinate rounds toward zero and lands back on the map
scripts/world/map_data.gd	s|if solid_tiles.has\(ground_at\(at\)\) or solid_tiles.has\(decor_at\(at\)\):|if true:|	tests/unit/test_map_data.gd	an unwalled map edge stops being reported
scripts/world/actor_body.gd	s|_shape.position = Vector2\(0.0, -config.body_size.y / 2.0\)|_shape.position = Vector2.ZERO|	tests/integration/test_world_movement.gd	the collision box sinks through the floor line
scripts/world/map_builder.gd	s|built.sorted.y_sort_enabled = true|built.sorted.y_sort_enabled = false|	tests/integration/test_world_movement.gd	actors stop sorting against the scenery
scripts/spritegen/tile_bank.gd	s|elif ch == TRANSPARENT_CHAR and not is_decor:|elif ch == TRANSPARENT_CHAR:|	tests/unit/test_tile_bank.gd	a decor tile is reported as a hole in the ground
scripts/spritegen/tile_bank.gd	s|elif ch == TRANSPARENT_CHAR and not is_decor:|elif false:|	tests/unit/test_tile_bank.gd	a hole in the ground stops being reported

# --- M4: interaction and dialog ---
scripts/util/input_gate.gd	s|if event == _last and frame == _frame:|if event == _last:|	tests/unit/test_input_gate.gd	a repeated press of the same key is swallowed forever
scripts/util/input_gate.gd	s|if event == _last and frame == _frame:|if false:|	tests/unit/test_input_gate.gd	one event is acted on twice in a frame
scripts/ui/dialog_runner.gd	s|if index < 0 or index >= choices.size\(\):|if false:|	tests/unit/test_dialog_runner.gd	an out-of-range choice is clamped instead of refused
scripts/ui/dialog_runner.gd	s|if not flag.is_empty\(\):|if false:|	tests/unit/test_dialog_runner.gd	a choice stops earning its flag
scripts/ui/dialog_runner.gd	s|if not _visible_choices\(node\).is_empty\(\):|if false:|	tests/unit/test_dialog_runner.gd	a line with choices advances and picks for the player
scripts/ui/dialog_runner.gd	s|if not requires.is_empty\(\) and not _flag_known\(requires\):|if false:|	tests/unit/test_dialog_runner.gd	a choice the player cannot take is offered anyway
scripts/world/interactor.gd	s|if to_target.dot\(forward\) <= 0.0:|if false:|	tests/unit/test_interactor.gd	you can talk to someone standing behind you
scripts/world/interactor.gd	s|if d > config.interact_reach \+ config.body_size.x:|if false:|	tests/unit/test_interactor.gd	interaction reaches across the whole map

# --- M5: saves, migrations and audio ---
scripts/autoload/save_manager.gd	s|backup.store_string\(text\)|pass|	tests/unit/test_saves.gd	an unreadable save is preserved as an empty file
scripts/data/migrations.gd	s|while version < SaveData.VERSION:|while false:|	tests/unit/test_saves.gd	old saves are handed on without being migrated
scripts/data/migrations.gd	s|var version := int\(d.get\(\"version\", 1\)\)|var version := int(d.get(\"version\", SaveData.VERSION))|	tests/unit/test_saves.gd	a save predating versioning is treated as current
scripts/data/migrations.gd	s|var d := raw.duplicate\(true\)|var d := raw|	tests/unit/test_saves.gd	migrating edits the file it was handed
scripts/data/save_data.gd	s|if version != VERSION:|if false:|	tests/unit/test_saves.gd	a save from a future version is read anyway
scripts/util/json_file.gd	s|out.append\(float\(v\)\)|out.append(float(int(v)))|	tests/unit/test_saves.gd	a fractional saved position is truncated

# --- M6: warps between maps ---
scripts/world/map_data.gd	s|if raw.size\(\) == 2 and Vector2i\(raw\[0\], raw\[1\]\) == at:|if false:|	tests/unit/test_map_data.gd	a warp tile stops being found

# --- M7: one source of truth for what gets scanned ---
scripts/util/lint_core.gd	s|"res://tools",|"res://nowhere",|	tests/unit/test_lint_core.gd	a source directory stops being linted, parsed and compiled
scripts/util/lint_core.gd	s|if not LINT_EXCLUDED_ROOTS.has\(root\):|if true:|	tests/unit/test_lint_core.gd	the suite gets linted, so every rule fixture becomes a violation
scripts/util/content_scan.gd	s|out.append_array\(_walk\(full, extensions\)\)|pass|	tests/unit/test_content_scan.gd	content in a subdirectory is never discovered
scripts/util/content_scan.gd	s|out.sort\(\)|pass|	tests/unit/test_content_scan.gd	discovery order stops being sorted, so generated art differs by machine

# --- M7: which game is this, as data ---
scripts/data/game_select.gd	s|if ids.size\(\) == 1:|if ids.size\(\) >= 1:|	tests/unit/test_game_select.gd	two games and no choice boots whichever was listed first
scripts/data/game_select.gd	s|if arg.begins_with\(ARG\):|if false:|	tests/unit/test_game_select.gd	--game= is ignored, so the QA harness tests the wrong game
scripts/data/game_select.gd	s|if not setting.is_empty\(\):|if false:|	tests/unit/test_game_select.gd	the committed project setting stops choosing the game
scripts/data/game_manifest.gd	s|if map.spawn\(start_spawn\) == Vector2i\(-1, -1\):|if false:|	tests/unit/test_game_manifest.gd	a game starting on a spawn that does not exist is accepted
scripts/data/game_manifest.gd	s|if not FileAccess.file_exists\(sheet\):|if false:|	tests/unit/test_game_manifest.gd	a player character with no generated art is accepted

# --- M7: the gameplay seam ---
scripts/world/interaction.gd	s|if hooks != null and hooks.on_interact\(ctx, target\):|if false:|	tests/unit/test_interaction.gd	game code never sees an interaction at all
scripts/world/interaction.gd	s|if hooks != null and hooks.on_interact\(ctx, target\):|if hooks != null:|	tests/unit/test_interaction.gd	a hook that refuses still swallows the interaction
scripts/world/interaction.gd	s|if once and ctx.was_seen\(key\):|if false:|	tests/unit/test_interaction.gd	a chest can be looted forever
scripts/world/interaction.gd	s|return "%s/%s" % \[map_id, object_id\]|return object_id|	tests/unit/test_interaction.gd	two maps share one chest, so the second is found already open
scripts/world/game_context.gd	s|ctx._flags = flags.duplicate\(\)|ctx._flags = flags|	tests/unit/test_interaction.gd	a hook holds live state instead of a snapshot
scripts/util/lint_core.gd	s|if check_autoload:|if false:|	tests/unit/test_lint_core.gd	game code names an autoload and drops out of the parse and compile gates
scripts/data/game_manifest.gd	s|out.append\("hooks script %s is not a GameHooks" % hooks.resource_path\)|pass|	tests/unit/test_game_manifest.gd	a hooks script that is not a GameHooks is accepted and never called

# --- M7: objects and locked doors ---
scripts/world/map_data.gd	s|if not String\(requires\).is_empty\(\) and not bool\(flags.get\(requires, false\)\):|if false:|	tests/unit/test_map_data.gd	a locked door opens without its key
scripts/world/map_data.gd	s|if taken.has\(object_id\):|if false:|	tests/unit/test_map_data.gd	two objects share an id, so the second is found already opened
scripts/world/map_data.gd	s|if says.is_empty\(\) and sets.is_empty\(\) and gives.is_empty\(\) and takes.is_empty\(\) and kind.is_empty\(\):|if false:|	tests/unit/test_map_data.gd	an object nobody can interact with ships as a dead button
scripts/world/map_data.gd	s|map.objects = file.get_array\("objects"\)|map.objects = []|	tests/unit/test_map_data.gd	a map stops carrying its objects at all

# --- M8: starting, tearing down and re-starting a game ---
scripts/world/world_scene.gd	s|box.closed.connect\(_on_dialog_closed\)|pass|	tests/integration/test_game_switch.gd	a rebuilt dialog box is never heard, so the player freezes after one conversation
scripts/world/world_scene.gd	s|GameState.reset\(\)|pass|	tests/integration/test_game_switch.gd	one game quest flags carry into the next and unlock its gates
scripts/world/world_scene.gd	s|_built.root.free\(\)|pass|	tests/integration/test_game_switch.gd	the previous game map and player survive the switch

# --- M9: grid-step movement ---
scripts/world/grid_walker.gd	s|if residual.dot\(_heading\) <= 0.0:|if false:|	tests/unit/test_grid_walker.gd	a step never notices it arrived and walks straight past the centre
scripts/world/grid_walker.gd	s|return _target$|return at|	tests/unit/test_grid_walker.gd	a step lands near the cell centre rather than exactly on it
scripts/world/grid_walker.gd	s|if residual.length\(\) > _planned - PROGRESS_EPSILON:|if false:|	tests/unit/test_grid_walker.gd	a step stopped by something never ends and the actor accepts no more input
scripts/world/grid_walker.gd	s|return abandon\(at\)|return at|	tests/unit/test_grid_walker.gd	a step stopped by a wall leaves the actor standing between cells
scripts/world/grid_walker.gd	s|out.y = _target.y|pass|	tests/unit/test_grid_walker.gd	a diagonal that slid along a wall is dragged back to the cell it started in
scripts/world/grid_walker.gd	s|tries.append\(Vector2i\(0, want.y\)\)|pass|	tests/unit/test_grid_walker.gd	a diagonal with one axis walled stops dead instead of taking the free one
scripts/world/grid_walker.gd	s|tries.append\(Vector2i\(want.x, 0\)\)|pass|	tests/unit/test_grid_walker.gd	a blocked diagonal breaks its tie vertically, against the rule Dir sets
scripts/world/grid_walker.gd	s|if not bool\(is_free.call\(to - at\)\):|if false:|	tests/unit/test_grid_walker.gd	a step is committed into a wall instead of being refused
scripts/world/grid_walker.gd	s|Dir.facing_from_vector\(residual, facing_now\)|free.facing|	tests/unit/test_grid_walker.gd	a key pressed mid-step turns the character while it walks the other way
scripts/data/game_config.gd	s|if grid_step_pixels < 0:|if false:|	tests/unit/test_grid_walker.gd	a negative grid step passes the config validator
scripts/world/actor_body.gd	s|GridWalker.new\(config\) if config.grid_step_pixels > 0 else null|null|	tests/integration/test_grid_movement.gd	grid movement never engages and the config field does nothing
scripts/world/actor_body.gd	s|global_position = _walker.settle\(global_position\)|pass|	tests/integration/test_grid_movement.gd	a step never lands, so the actor drifts past every cell centre
scripts/world/actor_body.gd	s|global_position = _walker.abandon\(global_position\)|pass|	tests/integration/test_grid_movement.gd	a dialog opening mid-step leaves the player standing between tiles
scripts/world/actor_body.gd	s|_walker.cancel\(\)|pass|	tests/integration/test_grid_movement.gd	a warp mid-step resolves it against the map the player just left
scripts/data/game_manifest.gd	s|style != null and config.grid_step_pixels != style.tile_size|false|	tests/unit/test_game_manifest.gd	a grid step that is not the map tile size is accepted

# --- M10: saves are per game ---
scripts/autoload/save_manager.gd	s|if data.game != game:|if false:|	tests/unit/test_saves.gd	a save from another game loads into this one's slot
scripts/autoload/save_manager.gd	s|\tvar glance := _read\(game, slot\)|\tvar glance := _read(game, slot) if load_slot(game, slot) == null else _read(game, slot)|	tests/unit/test_saves.gd	listing the slots parks files and announces loads nobody asked for
scripts/autoload/save_manager.gd	s|if arg.begins_with\(GameSelect.QA_ARG\):|if false:|	tests/unit/test_saves.gd	a scripted play session reads and writes a real player's saves
scripts/autoload/save_manager.gd	s|if not problems.is_empty\(\):|if false:|	tests/unit/test_saves.gd	a save with no game is written to a directory named after nothing
scripts/autoload/save_manager.gd	s|if held.exists and held.data == null:|if false:|	tests/unit/test_saves.gd	saving into a damaged slot destroys the bytes instead of parking them
scripts/data/migrations.gd	s|d\["game"\] = String\(game\)|d["game"] = ""|	tests/unit/test_saves.gd	a migrated save forgets which game it belongs to
scripts/data/save_data.gd	s|if String\(game\).is_empty\(\):|if false:|	tests/unit/test_saves.gd	a save that names no game passes validation
scripts/autoload/game_state.gd	s|out.game = game|pass|	tests/unit/test_saves.gd	a save forgets which game it came from
scripts/world/world_scene.gd	s|GameState.game = _game.id|pass|	tests/integration/test_game_switch.gd	nothing says which game is running, so no save can name one
scripts/autoload/router.gd	s|return str\(State.find_key\(_state\)\).to_lower\(\)|return "world"|	tests/unit/test_router.gd	every flow state reports itself as the world
scripts/data/game_config.gd	s|if save_slots < 1:|if false:|	tests/unit/test_game_config.gd	a game with no save slots passes the config validator

# --- M10: the pause menu ---
scripts/ui/pause_menu.gd	s|_index = posmod\(_index \+ delta, size\(\)\)|_index += delta|	tests/unit/test_pause_menu.gd	the pause cursor stops dead at the ends instead of wrapping
scripts/ui/pause_menu.gd	s|if _page == Page.LOAD and slot\(_index\) == null:|if false:|	tests/unit/test_pause_menu.gd	an empty slot offers to load, so confirming it loads nothing
scripts/ui/pause_menu.gd	s|if _page != Page.TOP:|if true:|	tests/unit/test_pause_menu.gd	backing out of the top page no longer resumes
scripts/world/world_scene.gd	s|Router.open_overlay\(Router.State.PAUSED\)|pass|	tests/integration/test_pause_and_saves.gd	pausing never takes control away from the player
scripts/world/world_scene.gd	s|if not at_tile.is_finite\(\):|if true:|	tests/integration/test_pause_and_saves.gd	a load drops the player on the spawn instead of where they saved
scripts/world/world_scene.gd	s|^\t_close_pause\(\)$|\tpass|	tests/integration/test_pause_and_saves.gd	the pause menu survives the load it asked for
scripts/world/world_scene.gd	s|_pause.free\(\)|pass|	tests/integration/test_pause_and_saves.gd	the pause menu outlives the game it was opened over
scripts/ui/pause_screen.gd	s|_committed = false|pass|	tests/integration/test_pause_and_saves.gd	a refused load leaves the menu on screen and deaf to every key

# --- M12: items and inventory ---
scripts/data/inventory.gd	s|if n <= 0 or count\(id\) < n:|if n <= 0:|	tests/unit/test_items.gd	a take bigger than the bag takes what it can and the count goes negative
scripts/data/inventory.gd	s|_counts.erase\(id\)|_counts[id] = 0|	tests/unit/test_items.gd	an item taken down to nothing is still listed as carried
scripts/data/inventory.gd	s|if n <= 0 or String\(id\).is_empty\(\):|if false:|	tests/unit/test_items.gd	giving zero of something, or an item with no id, is recorded
scripts/data/inventory.gd	s|return int\(d.get\(id, 0\)\) >= n|return int(d.get(id, 0)) > 0|	tests/unit/test_items.gd	a lock wanting three coins opens for one
scripts/data/item_def.gd	s|if String\(id\).is_empty\(\):|if false:|	tests/unit/test_items.gd	an item with no id passes validation and can never be looked up
scripts/data/item_def.gd	s|if name.is_empty\(\):|if false:|	tests/unit/test_items.gd	an item with no name draws as a blank row in the bag
scripts/autoload/game_state.gd	s|^\tinventory = Inventory.new\(\)$|\tpass|	tests/unit/test_items.gd	a new game starts holding the last one's items
scripts/autoload/game_state.gd	s|out.items = inventory.to_dict\(\)|pass|	tests/unit/test_saves.gd	a save forgets everything the player was carrying
scripts/data/save_data.gd	s|if int\(items\[key\]\) <= 0:|if false:|	tests/unit/test_saves.gd	a save carrying zero of an item is accepted rather than parked
scripts/data/migrations.gd	s|d\["items"\] = \{\}|pass|	tests/unit/test_saves.gd	a migrated save arrives with no inventory field at all
scripts/data/migrations.gd	s|d = _v3_to_v4\(d\)|break|	tests/unit/test_saves.gd	a v3 save is handed on unmigrated
scripts/world/game_context.gd	s|ctx._items = items.duplicate\(\)|ctx._items = items|	tests/unit/test_interaction.gd	a hook reads the live inventory instead of a snapshot of it
scripts/world/interaction.gd	s|if not needs.is_empty\(\) and not ctx.has_item\(StringName\(needs\), int\(record.get\("requires_count", 1\)\)\):|if false:|	tests/unit/test_interaction.gd	a lantern lights with no oil in it
scripts/world/interaction.gd	s|if not takes.is_empty\(\) and not ctx.has_item\(StringName\(takes\), take_count\):|if false:|	tests/unit/test_interaction.gd	an object consumes what the player has not got and is marked opened anyway
scripts/world/interaction.gd	s|ctx.say\(StringName\(locked\)\)|pass|	tests/unit/test_interaction.gd	a refused lock says nothing, so it reads as a dead button
scripts/world/interaction.gd	s|ctx.give_item\(StringName\(gives\), int\(record.get\("give_count", 1\)\)\)|pass|	tests/unit/test_interaction.gd	a chest with something in it hands over nothing
scripts/world/interaction.gd	s|ctx.take_item\(StringName\(takes\), take_count\)|pass|	tests/unit/test_interaction.gd	a consumable is never consumed
scripts/world/map_data.gd	s|if not String\(item\).is_empty\(\) and not Inventory.has_in\(items, item, int\(warp.get\("requires_count", 1\)\)\):|if false:|	tests/unit/test_map_data.gd	a door locked behind an item opens empty-handed
scripts/world/map_data.gd	s|"requires_item": StringName\(str\(warp.get\("requires_item", ""\)\)\),|"requires_item": \&"",|	tests/unit/test_map_data.gd	warp_at drops the item requirement, so nothing in play ever checks it
scripts/world/map_data.gd	s|if not str\(warp.get\("requires_item", ""\)\).is_empty\(\) and str\(warp.get\("locked_dialog", ""\)\).is_empty\(\):|if false:|	tests/unit/test_map_data.gd	a door locked behind an item with nothing to say ships
scripts/world/map_data.gd	s|if \(not needs.is_empty\(\) or not takes.is_empty\(\)\) and str\(object.get\("locked_dialog", ""\)\).is_empty\(\):|if false:|	tests/unit/test_map_data.gd	an object that can refuse and says nothing when it does ships
scripts/world/map_data.gd	s|_add_ref\(out, \(entry as Dictionary\).get\("requires_item", ""\)\)|pass|	tests/unit/test_map_data.gd	a door's key goes unlisted, so a misspelt item on a warp ships
scripts/ui/dialog_runner.gd	s|if not needs.is_empty\(\) and not Inventory.has_in\(_known_items, StringName\(needs\), int\(choice.get\("requires_count", 1\)\)\):|if false:|	tests/unit/test_dialog_runner.gd	a choice needing an item is offered to a player without it
scripts/ui/dialog_runner.gd	s|if not takes.is_empty\(\) and not Inventory.has_in\(_known_items, StringName\(takes\), int\(choice.get\("take_count", 1\)\)\):|if false:|	tests/unit/test_dialog_runner.gd	a choice that pays is offered to a player who cannot pay
scripts/ui/dialog_runner.gd	s| or flags_to_set\(\).has\(StringName\(name\)\)||	tests/unit/test_dialog_runner.gd	a gift hidden behind the flag it sets is handed over again on the next pass
scripts/ui/dialog_runner.gd	s|_effects.append\(\{"op": GameContext.OP_GIVE_ITEM, "id": StringName\(gives\),|_effects.append({"op": GameContext.OP_FLAG, "id": StringName(gives),|	tests/unit/test_dialog_runner.gd	a gift from a conversation is collected as something else entirely
scripts/ui/dialog_box.gd	s|var earned := _runner.effects\(\)|var earned := []|	tests/integration/test_world_items.gd	a conversation's promises and gifts are dropped as it closes
scripts/world/world_scene.gd	s|if Registry.get_resource\(&"ItemDef", item_id\) == null:|if false:|	tests/integration/test_world_items.gd	an item no data file describes is carried invisibly
scripts/world/world_scene.gd	s|elif GameState.give_item\(item_id, int\(effect.get\("count", 1\)\)\):|elif false:|	tests/integration/test_world_items.gd	a gift reaches the sink and nothing is given
scripts/world/world_scene.gd	s|if GameState.take_item\(StringName\(str\(effect.get\("id", ""\)\)\), int\(effect.get\("count", 1\)\)\):|if false:|	tests/integration/test_world_items.gd	a take never reaches the inventory
scripts/world/world_scene.gd	s|^\t_apply_effects\(effects\)$|\tpass|	tests/integration/test_world_items.gd	a closed conversation's effects never reach the one sink

# --- M12: the bag, on screen ---
scripts/ui/pause_menu.gd	s|return maxi\(_items.size\(\), 1\)|return _items.size()|	tests/unit/test_pause_menu.gd	an empty bag has no row, so the page cannot be stood on or escaped from
scripts/ui/pause_menu.gd	s|^\t\t\treturn Row.ITEMS$|\t\t\treturn Row.SAVE|	tests/unit/test_pause_menu.gd	backing out of the bag lands the cursor on Save instead of Items
scripts/ui/pause_menu.gd	s|\t\tif row == Row.ITEMS:|\t\tif false:|	tests/unit/test_pause_menu.gd	a game with no save slots cannot look in its own bag
scripts/ui/pause_menu.gd	s|return "%s%s x%d" % \[worn, row.name, row.count\]|return worn + row.name|	tests/unit/test_pause_menu.gd	the bag stops saying how many of a thing is in it
scripts/ui/pause_menu.gd	s|return "\(nothing carried\)"|return ""|	tests/unit/test_pause_menu.gd	an empty bag draws a blank row, which reads as a menu that failed
scripts/world/world_scene.gd	s|for item_id in GameState.inventory.ids\(\):|for item_id in []:|	tests/integration/test_pause_and_saves.gd	the pause menu never lists anything the player is carrying

# --- M13: the fight, as rules ---
scripts/ui/battle_logic.gd	s|return maxi\(1, attack - defense\)|return maxi(0, attack - defense)|	tests/unit/test_battle_logic.gd	armour that matches an attacker makes a fight nobody can end
scripts/ui/battle_logic.gd	s|var dealt := base \* 2 if timed else base|var dealt := base|	tests/unit/test_battle_logic.gd	a perfectly timed hit lands like any other
scripts/ui/battle_logic.gd	s|var taken := maxi\(base / 2, 1\) if blocked else base|var taken := base|	tests/unit/test_battle_logic.gd	blocking an enemy hit does nothing at all
scripts/ui/battle_logic.gd	s|if _pressed_at >= 0:|if false:|	tests/unit/test_battle_logic.gd	mashing the button re-arms the window, so timing means pressing a lot
scripts/ui/battle_logic.gd	s|return _count <= _combat.timed_window_frames|return true|	tests/unit/test_battle_logic.gd	the timing window is open for the whole cue
scripts/ui/battle_logic.gd	s|return _pressed_at >= 0 and _pressed_at <= _combat.timed_window_frames|return _pressed_at >= 0|	tests/unit/test_battle_logic.gd	any press at all counts as a timed one
scripts/ui/battle_logic.gd	s|moves\[_moves_rng.next_int\(0, moves.size\(\) - 1\)\]|moves[0]|	tests/unit/test_battle_logic.gd	an enemy only ever uses its first move, so half its content never ships
scripts/ui/battle_logic.gd	s|^\t\t\twho.hp = who.max_hp\(\)$|\t\t\tpass|	tests/unit/test_battle_logic.gd	levelling up stops healing, so the boss is unreachable
scripts/ui/battle_logic.gd	s|^\tvar barring := _boss\(\)$|\tvar barring := -1|	tests/unit/test_battle_logic.gd	a boss can be walked away from
scripts/ui/battle_logic.gd	s|_effects.append\(\{"op": GameContext.OP_SEEN, "key": _seen_key\}\)|pass|	tests/unit/test_battle_logic.gd	a beaten enemy is never marked beaten and comes back
scripts/ui/battle_logic.gd	s|var healed := mini\(on.hp \+ row.heal, on.max_hp\(\)\) - on.hp|var healed := row.heal|	tests/unit/test_battle_logic.gd	a tonic heals past the player's own maximum
scripts/ui/battle_logic.gd	s|_effects.append\(\{"op": GameContext.OP_TAKE_ITEM, "id": row.id, "count": 1\}\)|pass|	tests/unit/test_battle_logic.gd	a tonic is drunk and never consumed
scripts/ui/battle_logic.gd	s|if on.hp <= 0:|if false:|	tests/unit/test_battle_logic.gd	the player cannot lose a fight
scripts/data/combat_def.gd	s|return base_hp \+ hp_per_level \* \(maxi\(level, 1\) - 1\)|return base_hp|	tests/unit/test_combat_def.gd	levelling up adds no health
scripts/data/combat_def.gd	s|spent \+= step|pass|	tests/unit/test_combat_def.gd	every level costs the same as the first, so the curve is not a curve
scripts/data/combat_def.gd	s|if xp_curve.is_empty\(\):|if false:|	tests/unit/test_combat_def.gd	a game where nothing can ever level up ships unreported
scripts/data/enemy_def.gd	s|if moves.is_empty\(\):|if false:|	tests/unit/test_enemy_def.gd	an enemy with nothing to do on its turn ships
scripts/data/item_def.gd	s|if battle_heal < 0:|if false:|	tests/unit/test_items.gd	a potion that wounds the person who drinks it ships
scripts/data/migrations.gd	s|d\["party"\] = \{\}|pass|	tests/unit/test_saves.gd	a migrated save arrives with no party field at all
scripts/data/migrations.gd	s|d = _v4_to_v5\(d\)|break|	tests/unit/test_saves.gd	a v4 save is handed on without being migrated
scripts/data/save_data.gd	s|if leader_hp < 0 or \(leader_hp < 1 and companions.is_empty\(\)\):|if false:|	tests/unit/test_saves.gd	a save claiming a player at nought health is accepted
scripts/autoload/game_state.gd	s|out.party = \{\} if party_unset\(\) else \{|out.party = {} if true else {|	tests/unit/test_saves.gd	a save forgets everything the player fought for
scripts/autoload/game_state.gd	s|^\tplayer_level = 1$|\tpass|	tests/unit/test_saves.gd	a new game starts at the last one's level

# --- M13: the fight, on screen ---
scripts/world/map_data.gd	s|map.enemies = file.get_array\("enemies"\)|map.enemies = []|	tests/unit/test_map_data.gd	maps stop carrying the enemies written into them
scripts/world/map_data.gd	s|out.append\("enemy .%s. names no EnemyDef" % enemy_id\)|pass|	tests/unit/test_map_data.gd	an enemy naming no definition ships as a fight that never opens
scripts/world/map_data.gd	s|out.append\("enemy id .%s. is used twice" % enemy_id\)|pass|	tests/unit/test_map_data.gd	an enemy and a chest share one memory of being dealt with
scripts/world/map_data.gd	s|^\t\tfor named: StringName in _formation_of\(entry as Dictionary\):$|\t\tfor named: StringName in []:|	tests/unit/test_map_data.gd	a misspelt enemy id goes unlisted and ships
scripts/world/world_scene.gd	s|Router.open_overlay\(Router.State.BATTLE\)|pass|	tests/integration/test_world_battles.gd	a fight opens without taking control, so the player walks away mid-battle
scripts/world/world_scene.gd	s|open_game_over\(\)$|pass|	tests/integration/test_world_battles.gd	losing drops the player back into the world as if nothing happened
scripts/world/world_scene.gd	s|if _game == null or _game.combat == null or GameState.player_hp > 0:|if true:|	tests/integration/test_world_battles.gd	a new game starts its first fight with a player at nought health
scripts/ui/battle_screen.gd	s|_committed = true|pass|	tests/integration/test_world_battles.gd	a won fight pays out again on every frame that follows
scripts/ui/slot_menu.gd	s|if slot\(_index\) == null:|if false:|	tests/unit/test_title_menu.gd	an empty slot is loaded from the title or the game-over screen
scripts/ui/slot_menu.gd	s|if not _has_any_save\(\):|if false:|	tests/unit/test_title_menu.gd	a player with no saves is shown a list of nothing
scripts/world/map_data.gd	s|"enemy": StringName\(str\(enemy.get\("enemy", ""\)\)\),|"enemy": \&"",|	tests/unit/test_map_data.gd	the projection drops the enemy id, so every fight opens against nothing

# --- M13: the quest, reworked around the fighting ---
scripts/world/world_scene.gd	s|if GameState.was_seen\(Interaction.seen_key\(data.id, String\(enemy_id\)\)\):|if false:|	tests/integration/test_world_battles.gd	everything the player has beaten is standing on its tile again
games/quest/quest_hooks.gd	s|if ctx.was_seen\(SEEN_KEEPER\):|if false:|	tests/unit/test_quest_hooks.gd	the warden never acknowledges the thing she barred the gate against
data/combat/quest_combat.tres	s|xp_curve = Array\[int\]\(\[20, 24\]\)|xp_curve = Array[int]([20, 60])|	tests/unit/test_battle_content.gd	the curve drifts off the fights it was balanced against
data/enemies/keeper.tres	s|max_hp = 30|max_hp = 12|	tests/unit/test_battle_content.gd	the boss becomes beatable by mashing, so the timing is decoration
data/enemies/keeper.tres	s|^attack = 8$|attack = 80|	tests/unit/test_battle_content.gd	the boss becomes a wall no timing can answer, which the arithmetic model could not see
data/items/tonic.tres	s|battle_heal = 10|battle_heal = 0|	tests/unit/test_battle_content.gd	the tonic stops healing, so the Item command has nothing behind it
scripts/world/map_data.gd	s|_add_foe\(out, extra\)|_add_ref(out, extra)|	tests/unit/test_map_data.gd	a formation naming one enemy twice fields one of it, so no crowd can be same-species
data/maps/quest_village.json	s|"requires_flag": "rook_joins"|"requires_flag": "never_set"|	tests/unit/test_battle_content.gd	the west road opens to a player who declined the companion its fights are sized for
data/maps/quest_town.json	s|"requires_flag": "rook_joins"|"requires_flag": "never_set"|	tests/unit/test_battle_content.gd	the east road opens to a player who declined the companion its fights are sized for
data/party/scrapper.tres	s|joins_on_flag = &"rook_joins"|joins_on_flag = \&"never_set"|	tests/unit/test_battle_content.gd	the roads demand a flag that is no longer the one recruiting her
scripts/autoload/qa.gd	s|if screen.cue_on\(\) or screen.choosing\(\):|if screen.choosing():|	tests/fixtures/qa/quest/buy_a_tonic.json	fight_well stops pressing on the cue, so a played fight is mashed and the Keeper is unbeatable
scripts/ui/battle_screen.gd	s|return _logic != null and _logic.cue_on\(\)|return false|	tests/fixtures/qa/quest/finish_the_quest.json	the view reports the timing window as never open, so nothing scripted can play well
scripts/ui/battle_logic.gd	s|return _combat.defend_cue_frames|return _combat.attack_cue_frames|	tests/unit/test_battle_logic.gd	the enemy swing reports the player wind-up length, so the lean reads alike for both
scripts/ui/battle_logic.gd	s|^\t\t\treturn 1$|\t\t\treturn 0|	tests/unit/test_battle_logic.gd	a phase with no wind-up reports zero, and the view divides by it

# --- M13.2: the quest tells you where to go ---
games/quest/quest_hooks.gd	s|ctx.say\(DIALOG_ASKS\)|pass|	tests/unit/test_quest_hooks.gd	a new game opens with nobody having said what the player is for
games/quest/quest_hooks.gd	s|if ctx.map_id != START_MAP or ctx.has_flag\(FLAG_MET\):|if false:|	tests/unit/test_quest_hooks.gd	the opening conversation replays on every walk back into the village

# --- M13.3: the box states its capacity ---
scripts/ui/dialog_box.gd	s|const CHOICE_Y := TEXT_Y \+ TEXT_LINES \* LINE_HEIGHT \+ 2|const CHOICE_Y := TEXT_Y + 11|	tests/integration/test_dialog_box_layout.gd	choices are drawn back on top of the second line of text
scripts/ui/dialog_box.gd	s|_text.size = Vector2\(text_width\(_viewport.x\), TEXT_LINES \* LINE_HEIGHT\)|_text.size = Vector2(text_width(_viewport.x), LINE_HEIGHT)|	tests/integration/test_dialog_box_layout.gd	the text area shows one line of the two the writing is allowed
scripts/ui/dialog_box.gd	s|const TEXT_LINES := 2|const TEXT_LINES := 9|	tests/unit/test_dialog_fit.gd	the fit gate is told the box is nine lines tall and stops catching overflow
scripts/ui/dialog_box.gd	s|return CHOICE_Y \+ count \* CHOICE_PITCH \+ PAD_BOTTOM|return BOX_HEIGHT|	tests/integration/test_dialog_box_layout.gd	the box never grows, so the choices fall off the bottom of it

# --- M13.4: reading art without the import system ---
scripts/util/image_file.gd	s|if image.load_png_from_buffer\(bytes\) != OK:|if false:|	tests/unit/test_image_file.gd	an undecodable file reads as a blank image instead of as nothing

# --- M14: sound generated the way art is ---
scripts/soundgen/synth.gd	s|var q := clampi\(roundi\(samples\[i\] \* PEAK\), -32768, 32767\)|var q := roundi(samples[i] * PEAK)|	tests/unit/test_synth.gd	a sample past full scale wraps to the opposite sign instead of clamping
scripts/soundgen/synth.gd	s|return float\(left\) / float\(span\)|return 1.0|	tests/unit/test_synth.gd	every cue holds at full volume instead of decaying
scripts/soundgen/synth.gd	s|samples\[first \+ i\] \*= float\(n - i - 1\) / float\(n\)|samples[first + i] *= 1.0|	tests/unit/test_synth.gd	a cue stops ending on silence, so every play ends in a click
scripts/soundgen/procedural_sound_source.gd	s|return SeededRng.new\(base\).derive\(String\(cue_id\)\)|return SeededRng.new(base).derive("noise")|	tests/unit/test_sound_determinism.gd	every cue in a style draws the same noise
scripts/soundgen/procedural_sound_source.gd	s|var base := SeededRng.hash_seed\(0, String\(_style.id\)\)|var base := SeededRng.hash_seed(0, "")|	tests/unit/test_sound_determinism.gd	every voice shares one noise bed
scripts/soundgen/sound_bank.gd	s|if not cues.has\(cue\):|if false:|	tests/unit/test_sound_bank.gd	a bank missing a cue the template asks for passes validation
scripts/soundgen/sound_bank.gd	s|if Sfx.of\(cue\) < 0:|if false:|	tests/unit/test_sound_bank.gd	a cue left behind by a rename is generated forever with nothing playing it
scripts/util/sfx.gd	s|Cue.FOOTSTEP: &"footstep",||	tests/unit/test_sfx.gd	a cue in the enum has no id, so it silently resolves to no sound
scripts/util/sfx.gd	s|return -1|return 0|	tests/unit/test_sfx.gd	a sound name no cue has resolves to the first cue instead of being refused
scripts/data/sound_style.gd	s|if not Synth.TONES.has\(tone\):|if false:|	tests/unit/test_sound_bank.gd	a voice naming a waveform that does not exist passes validation

# --- M14: resolving a sound, across two roots ---
scripts/util/content_scan.gd	s|return file_name.trim_suffix\(".remap"\).trim_suffix\(".import"\)|return file_name.trim_suffix(".remap")|	tests/unit/test_content_scan.gd	imported assets become invisible to every scan in an exported build
scripts/autoload/audio_bus.gd	s|\t\t_sounds\[id\] = stream|\t\tif not _sounds.has(id): _sounds[id] = stream|	tests/unit/test_audio_bus.gd	a generated cue beats the file a game dropped in to replace it
scripts/autoload/audio_bus.gd	s|_next = \(_next \+ 1\) % _players.size\(\)|_next = 0|	tests/unit/test_audio_bus.gd	every cue cuts the one before it again
scripts/autoload/audio_bus.gd	s|if seen.has\(id\):|if false:|	tests/unit/test_audio_bus.gd	two files answering to one name pass in silence, and which wins depends on the filesystem
scripts/autoload/audio_bus.gd	s|if _requested.size\(\) > LOG_LIMIT:|if false:|	tests/unit/test_audio_bus.gd	the request log grows without bound in an autoload that outlives every scene
scripts/autoload/audio_bus.gd	s|\tif not _sounds.has\(cue\):|\tif false:|	tests/unit/test_audio_bus.gd	a voice that cannot play a cue stops reporting it
scripts/data/game_manifest.gd	s|if not FileAccess.file_exists\(cue\):|if false:|	tests/unit/test_game_manifest.gd	a game pointed at a voice with no generated cues passes validation
scripts/util/content_scan.gd	s|return in_order\(once_each\(_walk\(root, extensions\)\)\)|return in_order(_walk(root, extensions))|	tests/unit/test_content_scan.gd	a source and its import sidecar count as two files, doubling every work list

# --- M14: the template gets a voice ---
scripts/world/step_meter.gd	s|\t_carried -= _stride|\t_carried = 0.0|	tests/unit/test_step_meter.gd	the leftover distance is thrown away, so the cadence follows the frame rate
scripts/world/step_meter.gd	s|\t_carried = 0.0|\tpass|	tests/unit/test_step_meter.gd	being teleported counts as ground walked and lands a footstep on arrival
scripts/world/actor_body.gd	s|\t\tstep.footfall = _meter.advance\(was.distance_to\(global_position\)\)|\t\tstep.footfall = false|	tests/integration/test_world_sounds.gd	walking makes no sound at all
scripts/world/actor_body.gd	s|\t\t_meter.reset\(\)|\t\tpass|	tests/integration/test_world_sounds.gd	a warp counts the distance across the map as a stride
scripts/ui/battle_logic.gd	s|_want\(Sfx.Cue.TIMED_HIT if timed else Sfx.Cue.HIT\)|_want(Sfx.Cue.HIT)|	tests/unit/test_battle_sounds.gd	landing the timing sounds exactly like missing it
scripts/ui/battle_logic.gd	s|\t_sounds.clear\(\)|\tpass|	tests/unit/test_battle_sounds.gd	a drained cue plays again every frame, turning one hit into a stutter
scripts/ui/battle_logic.gd	s|\t\t_want\(Sfx.Cue.LEVEL_UP\)|\t\tpass|	tests/unit/test_battle_sounds.gd	levelling up is silent
scripts/ui/dialog_box.gd	s|\t\tsound_wanted.emit\(Sfx.id_of\(Sfx.Cue.PAGE\)\)|\t\tpass|	tests/integration/test_world_sounds.gd	turning a page of dialog is silent
scripts/ui/dialog_box.gd	s|\t\t\tsound_wanted.emit\(Sfx.id_of\(Sfx.Cue.TALK\)\)|\t\t\tpass|	tests/integration/test_world_sounds.gd	the typewriter has no voice
scripts/world/world_scene.gd	s|\t\tAudioBus.play\(Sfx.Cue.LOCKED\)|\t\tpass|	tests/integration/test_world_sounds.gd	a door that refuses to open gives no feedback for it

# --- M14: turning it down ---
scripts/autoload/settings.gd	s|\t_level = \(\(_level \+ 1\) % Level.size\(\)\) as Level|\t_level = Level.NORMAL|	tests/unit/test_settings.gd	the volume row stops changing anything
scripts/autoload/settings.gd	s|\tAudioBus.set_volume\(sound_gain\(\)\)|\tpass|	tests/unit/test_settings.gd	the menu says Quiet while nothing gets any quieter
scripts/autoload/settings.gd	s|\tif raw < 0 or raw >= Level.size\(\):|\tif false:|	tests/unit/test_settings.gd	a settings file from the future silences the game with no way to find out why
scripts/autoload/settings.gd	s|\tvar err := JsonFile.write\(_path, \{"sound_level": int\(_level\)\}\)|\tvar err := OK|	tests/unit/test_settings.gd	the chosen volume never survives a restart
scripts/autoload/settings.gd	s|\t\treturn QA_PATH|\t\treturn DEFAULT_PATH|	tests/unit/test_settings.gd	a scripted play session writes the player's real settings
scripts/ui/pause_menu.gd	s|\t\tif row == Row.SOUND:|\t\tif false:|	tests/unit/test_pause_menu.gd	a game with no save slots cannot turn the sound down
scripts/ui/pause_screen.gd	s|\t\t\treturn _menu.sound_label\(\) if row == PauseMenu.Row.SOUND else TOP_LABELS\[row\]|\t\t\treturn TOP_LABELS[row]|	tests/integration/test_pause_and_saves.gd	the sound row draws as an empty line
tools/compile_all.gd	s|\t\tif at == 0 or not _is_identifier_char\(code\[at - 1\]\):|\t\tif true:|	tests/unit/test_compile_all.gd	a singleton whose name ends another identifier drops files out of the compile gate

# --- M15: the played game, and the artifact it ships as ---
scripts/ui/battle_logic.gd	s|return maxi\(1, attack - defense\)|return maxi(1, attack - defense + 3)|	tests/fixtures/qa/quest/finish_the_quest.json	combat arithmetic changes and the PLAYED game does not notice
scripts/ui/battle_logic.gd	s|\t\t\twho.hp = who.max_hp\(\)|\t\t\tpass|	tests/fixtures/qa/quest/finish_the_quest.json	levelling stops healing, which the played game only sees because it now reads hp mid-run
scripts/spritegen/tile_bank.gd	s|if row.length\(\) != tile:|if false:|	tests/unit/test_tile_bank.gd	a ragged tile row stops being reported
scripts/spritegen/tile_bank.gd	s|if rows.size\(\) != tile:|if false:|	tests/unit/test_tile_bank.gd	a tile that is not the bank's size stops being reported
scripts/spritegen/tile_bank.gd	s|if not known_chars.contains\(ch\):|if false:|	tests/unit/test_tile_bank.gd	a typo'd pixel character stops being reported
scripts/spritegen/tile_bank.gd	s|elif seen.has\(tile_id\):|elif false:|	tests/unit/test_tile_bank.gd	two tiles answering to one name stop being reported
scripts/spritegen/tile_bank.gd	s|if not override.is_empty\(\):|if false:|	tests/unit/test_tile_bank.gd	a style stops being able to recolour one tile of a shared bank
scripts/spritegen/tile_gen.gd	s|if bank.tile != style.tile_size:|if false:|	tests/unit/test_tile_bank.gd	pixel art authored at another size is silently scaled
scripts/spritegen/tile_gen.gd	s|img.set_pixel\(x, y, outline\)|pass|	tests/unit/test_tile_bank.gd	the outline character stops drawing anything
scripts/spritegen/tile_gen.gd	s|var tone := TileBank.TONE_CHARS.find\(ch\)|var tone := -1|	tests/unit/test_tiles.gd	authored rows stop reaching the drawn tile
scripts/world/npc_brain.gd	s|return kind != Kind.STATIC|return true|	tests/integration/test_world_npcs.gd	every statue in the game is handed a brain, including the ones QA sessions walk into
scripts/world/npc_brain.gd	s|for dx in range\(-_range, _range \+ 1\):|for dx in range(-9, 10):|	tests/unit/test_npc_brain.gd	the wander bound stops binding and the shopkeeper leaves the shop
scripts/world/npc_brain.gd	s|_index = \(_index \+ 1\) % _path.size\(\)|_index = 0|	tests/unit/test_npc_brain.gd	a patrol walks to its first waypoint forever instead of round its route
scripts/world/map_data.gd	s|if kind < 0:|if false:|	tests/unit/test_map_data.gd	a typo'd behavior stands still instead of failing the build
scripts/world/map_data.gd	s|if solid_tiles.has\(ground_at\(point\)\) or solid_tiles.has\(decor_at\(point\)\):|if false:|	tests/unit/test_map_data.gd	a patrol waypoint inside a wall is accepted and the NPC walks into it forever
scripts/world/world_scene.gd	s|\t_drive_npcs\(\)|\tpass|	tests/integration/test_world_npcs.gd	behaviour is read from the map and then nothing ever moves
scripts/world/actor_body.gd	s|platform_floor_layers = 0|platform_floor_layers = 0xFFFFFFFF|	tests/integration/test_world_npcs.gd	an NPC walking into the player from the north is dragged wherever the player strafes
scripts/autoload/game_state.gd	s|if n <= 0 or n > gold:|if false:|	tests/unit/test_saves.gd	a player buys what they cannot afford and the purse goes negative
scripts/autoload/game_state.gd	s|gold -= n|pass|	tests/unit/test_saves.gd	spending pays for nothing and money is infinite
scripts/data/migrations.gd	s|d\["gold"\] = 0|pass|	tests/unit/test_saves.gd	an old save loads with no purse at all
scripts/ui/battle_logic.gd	s|if _outcome == Outcome.VICTORY and purse > 0:|if false:|	tests/unit/test_battle_logic.gd	winning a fight pays nothing
scripts/data/save_data.gd	s|if gold < 0:|if false:|	tests/unit/test_saves.gd	a hand-edited save carrying a negative purse loads without complaint
scripts/ui/shop_menu.gd	s|if _page == Page.BUY and not affordable\(_index\):|if false:|	tests/unit/test_shop_menu.gd	a player buys what they cannot afford and the purse decides afterwards
scripts/ui/shop_menu.gd	s|return r != null and r.price <= _gold|return r != null|	tests/unit/test_shop_menu.gd	every row reads as affordable, so the refusal never fires
scripts/ui/shop_menu.gd	s|return maxi\(buy_price / 2, 1\)|return buy_price|	tests/unit/test_shop_menu.gd	a shop pays full price for what it resells and money is infinite
scripts/ui/shop_menu.gd	s|_index = Row.BUY if _page == Page.BUY else Row.SELL|_index = 0|	tests/unit/test_shop_menu.gd	backing out of a page drops the player at the top of a counter they were halfway down
scripts/data/shop_def.gd	s|elif seen.has\(item_id\):|elif false:|	tests/unit/test_items.gd	a shop stocks the same thing twice and the cursor lies about what it points at
scripts/ui/shop_menu.gd	s|return price > 0|return true|	tests/unit/test_shop_menu.gd	unpriced quest items are for sale and a key can be sold away
scripts/ui/shop_menu.gd	s|_count = clampi\(_count \+ delta, 1, limit\(\)\)|_count = _count + delta|	tests/unit/test_shop_menu.gd	the count runs past what the purse can cover and the keeper offers a deal it must refuse
scripts/ui/shop_menu.gd	s|return maxi\(_gold / maxi\(r.price, 1\), 1\)|return 99|	tests/unit/test_shop_menu.gd	a player buys eleven tonics with three tonics worth of gold
scripts/ui/shop_menu.gd	s|return 0 if r == null or _count <= 0 else r.price \* _count|return 0 if r == null else r.price|	tests/unit/test_shop_menu.gd	the total stops multiplying, so the player agrees to one number and pays another
scripts/ui/shop_menu.gd	s|_saying = Saying.POOR|pass|	tests/unit/test_shop_menu.gd	a refusal makes no words, which reads as a dead key
scripts/ui/shop_menu.gd	s|return r.description if r != null else ""|return ""|	tests/unit/test_shop_menu.gd	the description bar goes blank and the shop stops saying what it sells
scripts/ui/shop_menu.gd	s|_saying = Saying.HOW_MANY|pass|	tests/unit/test_shop_menu.gd	the keeper stops asking how many and the count runs unannounced
scripts/ui/battle_logic.gd	s|who.attack_mod \+ who.status.attack_bonus\(\)|who.status.attack_bonus()|	tests/unit/test_battle_logic.gd	a sword changes nothing and gear is decoration
scripts/ui/battle_logic.gd	s|who.defense_mod \+ who.status.defense_bonus\(\)|who.status.defense_bonus()|	tests/unit/test_battle_logic.gd	armour changes nothing and gear is decoration
scripts/autoload/game_state.gd	s|if wearers_of\(id\) - already \+ 1 > inventory.count\(id\):|if false:|	tests/unit/test_saves.gd	the player equips a sword they do not own
scripts/autoload/game_state.gd	s|\t_strip_phantom_markers\(id\)|\tpass|	tests/unit/test_saves.gd	the last sword leaves the bag and stays equipped as a phantom
scripts/data/migrations.gd	s|d\["equipment"\] = \{\}|pass|	tests/unit/test_saves.gd	an old save loads with no equipment map at all
scripts/data/save_data.gd	s|if int\(items.get\(id, 0\)\) <= 0:|if false:|	tests/unit/test_saves.gd	a hand-edited save equipping what it does not carry loads without complaint
scripts/data/item_def.gd	s|if slot != &"" and not SLOTS.has\(slot\):|if false:|	tests/unit/test_items.gd	a typo'd slot name becomes a trinket that never equips
scripts/ui/pause_menu.gd	s|\t\tif row.slot == _pick_slot:|\t\tif true:|	tests/unit/test_pause_menu.gd	a tonic is offered as armour, because the candidate list stopped filtering
scripts/ui/pause_menu.gd	s|var worn := "\(E\) " if row.equipped else ""|var worn := ""|	tests/unit/test_pause_menu.gd	nothing in the bag says which sword is the one you are holding
scripts/world/world_scene.gd	s|if GameState\.is_equipped\(item\.id\):|if false:|	tests/integration/test_world_equipment.gd	the sword on your own back is offered for sale
scripts/world/world_scene.gd	s|\treturn "Take off: %s  \(%s\)" % \[_delta_words\(-worn.attack, -worn.defense\), _totals_words\(\)\]|\treturn ""|	tests/integration/test_world_equipment.gd	taking gear off promises nothing, so the row is a press into silence
scripts/ui/pause_menu.gd	s|\t\tif row == Row.EQUIP:|\t\tif false:|	tests/unit/test_pause_menu.gd	a game with no save slots cannot open its own wardrobe
scripts/ui/pause_menu.gd	s|\t\t_pick_slot = slot_row.slot_id|\t\tpass|	tests/unit/test_pause_menu.gd	picking a slot opens a candidate list that is answering about nowhere
scripts/ui/pause_menu.gd	s|\t\t\tvar answer := Pick.of\(Kind.UNEQUIP, -1, &"", _pick_slot\)|\t\t\tvar answer := Pick.of(Kind.NONE)|	tests/unit/test_pause_menu.gd	the take-off row is decoration, so gear once worn is worn forever
scripts/ui/pause_menu.gd	s|\t\t\tif pick_worn_name\(\).is_empty\(\):|\t\t\tif false:|	tests/unit/test_pause_menu.gd	taking off an empty slot is asked for, and the world shrugs
scripts/ui/pause_menu.gd	s|\t\tvar worn_answer := Pick.of\(Kind.EQUIP, -1, chosen.id\)|\t\tvar worn_answer := Pick.of(Kind.NONE)|	tests/unit/test_pause_menu.gd	a confirm on a candidate equips nothing
scripts/ui/pause_menu.gd	s|^\t_page = Page.EQUIP$|\tpass|	tests/unit/test_pause_menu.gd	equipping strands the cursor on a candidate list built from a bag that just changed
scripts/ui/pause_menu.gd	s|^\t\t\treturn Row.EQUIP$|\t\t\treturn Row.SAVE|	tests/unit/test_pause_menu.gd	backing out of the wardrobe lands the cursor on Save
scripts/ui/pause_menu.gd	s|\t\treturn "%s: \(nothing\)" % row.label|\t\treturn ""|	tests/unit/test_pause_menu.gd	an empty slot draws a blank row, which reads as a menu that failed
scripts/ui/pause_menu.gd	s|\t\treturn "\(take off\)"|\t\treturn ""|	tests/unit/test_pause_menu.gd	the one row that removes gear is drawn as an empty line
scripts/world/world_scene.gd	s|\tGameState.equip\(def.slot, item_id, _pause_member\)|\tpass|	tests/integration/test_world_equipment.gd	a confirm on the equipment screen never reaches the slot map
scripts/world/world_scene.gd	s|\tGameState.unequip\(slot, _pause_member\)|\tpass|	tests/integration/test_world_equipment.gd	the world hears the take-off and leaves the gear on
scripts/world/world_scene.gd	s|\t\tattack_delta -= current.attack|\t\tpass|	tests/integration/test_world_equipment.gd	a candidate promises its own stats again instead of the swap it would be
scripts/world/world_scene.gd	s|\t\tgrowth.attack_at\(grown\), _equip_mod\(&"attack", member\),|\t\t0, 0,|	tests/integration/test_world_equipment.gd	the equip screen stops saying what the player's numbers actually are
scripts/ui/pause_screen.gd	s|\t_stats.visible = equipping and not _menu.stats_label\(\).is_empty\(\)|\t_stats.visible = false|	tests/integration/test_pause_and_saves.gd	the stats readout never draws, so a delta has nothing to be a delta of
scripts/ui/pause_menu.gd	s|\t\tif row == Row.STATUS:|\t\tif false:|	tests/unit/test_pause_menu.gd	a game with no save slots cannot ask how its player is doing
scripts/ui/pause_menu.gd	s|\t\treturn "\(nothing to report\)"|\t\treturn ""|	tests/unit/test_pause_menu.gd	a status page with nothing to say draws blanks, which reads as a failed draw
scripts/ui/pause_menu.gd	s|^\t\t\treturn Row.STATUS$|\t\t\treturn Row.SAVE|	tests/unit/test_pause_menu.gd	backing out of the status page lands the cursor on Save
scripts/world/world_scene.gd	s|\t\tout.append\("Level %d" % level\)|\t\tpass|	tests/integration/test_world_equipment.gd	the status page never says what level the player is
scripts/world/world_scene.gd	s|\t\tout.append\("HP %d/%d" % \[hp, curve.max_hp\(level\)\]\)|\t\tpass|	tests/integration/test_world_equipment.gd	nothing outside a fight says how hurt the player is
scripts/world/world_scene.gd	s|\t\t\tout.append\("XP %d  \(next in %d\)" % \[|\t\t\tout.append("" % [|	tests/integration/test_world_equipment.gd	the line a player opens the status page FOR is missing
scripts/world/world_scene.gd	s|\t\tout.append\(PauseMenu.gear_label\(row\)\)|\t\tpass|	tests/integration/test_world_equipment.gd	the status page cannot tell you what you are wearing
scripts/ui/dialog_runner.gd	s|\tif not _can_afford\(choice\):|\tif false:|	tests/unit/test_dialog_runner.gd	a player with no coin sleeps anyway and the purse goes wherever it likes
scripts/ui/dialog_runner.gd	s|\treturn spend \+ _gold_committed\(\) <= _known_gold|\treturn spend <= _known_gold|	tests/unit/test_dialog_runner.gd	ten gold pays for two six-gold nights, because each is checked against an untouched purse
scripts/ui/dialog_runner.gd	s|\t\t_effects.append\(\{"op": GameContext.OP_SPEND_GOLD, "amount": spend\}\)|\t\tpass|	tests/unit/test_dialog_runner.gd	the keeper is agreed with and never paid
scripts/ui/dialog_runner.gd	s|\t\t_effects.append\(\{"op": GameContext.OP_REST\}\)|\t\tpass|	tests/unit/test_dialog_runner.gd	the player pays for a night and wakes up as hurt as they went to bed
scripts/ui/dialog_runner.gd	s|\t\t\tif costs > 0 and refusal.is_empty\(\):|\t\t\tif false:|	tests/unit/test_dialog_runner.gd	a keeper charges money and says nothing at all when the player cannot pay
scripts/ui/dialog_runner.gd	s|\t\t_collect_reachable\(str\(\(entry as Dictionary\).get\("poor_next", ""\)\), seen\)|\t\tpass|	tests/unit/test_dialog_runner.gd	every correctly-written refusal fails the build as unreachable
scripts/world/world_scene.gd	s|\t\t\t\tif GameState.spend_gold\(int\(effect.get\("amount", 0\)\)\):|\t\t\t\tif false:|	tests/integration/test_world_items.gd	a spend reaches the sink and the money stays in the purse
scripts/world/world_scene.gd	s|_game.combat.max_hp\(level\), GameState.player_xp, level,|GameState.player_hp, GameState.player_xp, level,|	tests/integration/test_world_items.gd	a night at the inn costs money and heals nothing
scripts/world/world_scene.gd	s|\topen_rest.call_deferred\(\)|\tpass|	tests/fixtures/qa/quest/rest_at_the_inn.json	the player pays for a room and the night never comes
scripts/world/world_scene.gd	s|\tRouter.open_overlay\(Router.State.RESTING\)|\tpass|	tests/fixtures/qa/quest/rest_at_the_inn.json	a night passes with the player free to walk out through the middle of it
scripts/ui/rest_screen.gd	s|\t\t_done = true|\t\tpass|	tests/unit/test_rest_screen.gd	a night announces it is over on every frame, not once
scripts/ui/rest_screen.gd	s|\t\t_shade.color.a = float\(_elapsed\) / float\(_fade_frames\)|\t\t_shade.color.a = 0.0|	tests/unit/test_rest_screen.gd	sleep never darkens anything, so a night reads as the game freezing
scripts/ui/slot_menu.gd	s|\t_index = Row.CONTINUE if _has_any_save\(\) else Row.NEW_GAME|\t_index = Row.CONTINUE|	tests/unit/test_title_menu.gd	a player's first press of the game bounces off a row that refuses
scripts/ui/slot_menu.gd	s|\t\tvar answer := top_pick\(_index\)|\t\tvar answer: Pick = null|	tests/unit/test_game_over_menu.gd	the game-over screen's own way back to the title answers nothing
scripts/ui/game_over_menu.gd	s|\treturn ROW_TITLE \+ 1|\treturn Row.size()|	tests/unit/test_game_over_menu.gd	the Title row exists and is never drawn, so nobody can reach it
scripts/ui/title_screen.gd	s|\t\t\tload_requested.emit\(pick.slot\)|\t\t\tpass|	tests/integration/test_title.gd	Continue is pressed at the title and no save is ever loaded
scripts/ui/title_screen.gd	s|\t\t\tnew_game_requested.emit\(\)|\t\t\tpass|	tests/fixtures/qa/quest/the_title_screen.json	New game is pressed and the title just sits there
scripts/world/world_scene.gd	s|\tif not open_title\(\):|\tif true:|	tests/integration/test_title.gd	the game boots straight into a map and the title is never seen
scripts/world/world_scene.gd	s|\t_title.setup\(TitleMenu.of\(_slot_summaries_for\(_offered\)\), style, _ui_size\(\),|\t_title.setup(TitleMenu.of([] as Array[SaveData]), style, _ui_size(),|	tests/integration/test_title.gd	the title never sees the saves, so Continue is refused to a player who has one
scripts/world/world_scene.gd	s|^\topen_title\(\)$|\tpass|	tests/integration/test_title.gd	the game-over screen's Title row leads nowhere
scripts/world/world_scene.gd	s|\treturn restore\(data\)|\treturn enter_map(_game.start_map, _game.start_spawn)|	tests/integration/test_title.gd	Continue starts a new game wearing the save's name
scripts/autoload/router.gd	s|\tset_state\(base\)|\t_state = base|	tests/unit/test_router.gd	a run starts and the state machine tells nobody, the way it did before M23
scripts/autoload/router.gd	s|\t_to_base\(State.TITLE\)|\t_to_base(State.WORLD)|	tests/unit/test_router.gd	going back to the title lands in the world instead
scripts/world/world_scene.gd	s|\t_close_battle\(\)|\tpass|	tests/integration/test_flow_model.gd	a defeat stacks the game over on top of the fight screen it never closed
scripts/util/flow_doc.gd	s|\t\tif from == to:|\t\tif false:|	tests/unit/test_flow_doc.gd	the diagram draws a warp as a state change, so the picture disagrees with the model
scripts/soundgen/tune.gd	s|\t\thz \*= 2.0|\t\tpass|	tests/unit/test_tune.gd	every octave plays the same note
scripts/soundgen/tune.gd	s|\t\tmix\[i\] = clampf\(v, -1.0, 1.0\)|\t\tmix[i] = v|	tests/unit/test_tune.gd	a chord past full scale wraps to the opposite sign and cracks
scripts/soundgen/tune.gd	s|\t\t\tv = Synth.quantise\(v, style.quantise_steps\)|\t\t\tpass|	tests/unit/test_tune.gd	the bit-crush stops reaching the mix, so a crushed voice plays clean
scripts/soundgen/synth.gd	s|\tif fall <= release:|\tif false:|	tests/unit/test_tune.gd	a note ends at full volume, so every loop seam is a click
scripts/soundgen/music_track.gd	s|\treturn \(step \* mix_rate \* 60\) / maxi\(bpm \* per_beat, 1\)|\treturn step * mix_rate / maxi(bpm, 1)|	tests/unit/test_music_track.gd	a note's start drifts off the beat as the tune goes on
scripts/soundgen/music_track.gd	s|\t\tif lengths\[bar\] != steps_per_bar\(\):|\t\tif false:|	tests/unit/test_music_track.gd	a bar that does not add up renders short and every bar after it walks off the beat
scripts/soundgen/music_track.gd	s|\tif Sfx.of\(id\) >= 0:|\tif false:|	tests/unit/test_music_track.gd	a track named after a cue shadows it in the one table both share
scripts/soundgen/music_track.gd	s|\tif seconds\(\) > MAX_SECONDS:|\tif false:|	tests/unit/test_music_track.gd	a track of any length is committed, three voices at a time
scripts/autoload/audio_bus.gd	s|\t\twav.loop_mode = AudioStreamWAV.LOOP_FORWARD|\t\tpass|	tests/unit/test_audio_bus.gd	the theme plays once and the game goes quiet
scripts/autoload/audio_bus.gd	s|\tif id == _music_id:|\tif false:|	tests/unit/test_audio_bus.gd	crossing between two rooms that share a theme restarts it from the top
scripts/autoload/audio_bus.gd	s|\t\t_remember\(id\)|\t\tpass|	tests/unit/test_audio_bus.gd	nothing records what was asked for, so no gate can see a misspelled cue
scripts/autoload/audio_bus.gd	s|\tif not _play\(_music, id, false\):|\tif not _play(_music, id):|	tests/unit/test_audio_bus.gd	a track is logged as a cue again, and every scripted session fails on the tune
scripts/autoload/audio_bus.gd	s|^\t\tstop_music\(\)$|\t\tpass|	tests/integration/test_world_music.gd	a dungeon inherits whatever the last map was playing
scripts/world/world_scene.gd	s|\t\tAudioBus.play_music\(_offered.title_music\)|\t\tpass|	tests/integration/test_world_music.gd	the title screen is silent
scripts/data/game_manifest.gd	s|\t\tif not FileAccess.file_exists\(track\):|\t\tif false:|	tests/unit/test_game_manifest.gd	a game names a theme nobody generated and the title is quietly silent
.github/workflows/ci.yml	s|^      - "docs/FLOW.md" #.*$|      # removed|	tests/unit/test_ci_paths.gd	a hand-edit to the generated flow doc, pushed to main, runs no gate at all
tools/ci_changed.sh	s|      docs/FLOW.md\) needs=true ;;|      docs/FLOW.md) ;;|	tests/unit/test_ci_paths.gd	the generated flow doc skips the gate, so a hand-edit lands and every later run goes red
tools/ci_changed.sh	s|      docs/\*\) ;;|      tests/*) ;;|	tests/unit/test_ci_paths.gd	a test change is read as prose and gets a green check that tested nothing at all

# --- M25: magic - the resource layer ---
scripts/data/combat_def.gd	s|return base_mp \+ mp_per_level \* \(maxi\(level, 1\) - 1\)|return base_mp|	tests/unit/test_combat_def.gd	magic stops growing with level, so every caster is a level-1 one forever
scripts/data/combat_def.gd	s|if base_mp < 0 or mp_per_level < 0:|if false:|	tests/unit/test_combat_def.gd	a game can ship negative magic and the build says nothing
scripts/data/spell_def.gd	s|if mp_cost <= 0:|if false:|	tests/unit/test_spell_def.gd	a free spell ships, and casting stops being a decision
scripts/data/spell_def.gd	s|if learn_level < 1:|if false:|	tests/unit/test_spell_def.gd	a spell learned below level one ships
scripts/data/spell_def.gd	s|if kind < 0 or kind >= Kind.size\(\):|if false:|	tests/unit/test_spell_def.gd	a hand-edited kind no spell has costs the player a turn doing nothing
scripts/data/spell_def.gd	s|\t\tif status_turns <= 0:|\t\tif false:|	tests/unit/test_spell_def.gd	a sleep that takes no turns away ships as a spell that does nothing
scripts/data/spell_def.gd	s|\telif power <= 0:|\telif false:|	tests/unit/test_spell_def.gd	an attack spell with no power ships and reads as a broken button
scripts/autoload/game_state.gd	s|\tplayer_mp = maxi\(mp, 0\)|\tpass|	tests/unit/test_saves.gd	the one party writer drops the magic it was handed
scripts/autoload/game_state.gd	s|\"hp\": player_hp, \"xp\": player_xp, \"level\": player_level, \"mp\": player_mp,|\"hp\": player_hp, \"xp\": player_xp, \"level\": player_level,|	tests/unit/test_saves.gd	a save records no magic, so every load empties the caster
scripts/autoload/game_state.gd	s|\tplayer_mp = maxi\(int\(data.party.get\(\"mp\", 0\)\), 0\)|\tpass|	tests/unit/test_saves.gd	a load ignores the magic in the file it just read
scripts/data/migrations.gd	s|\t\tparty\[\"mp\"\] = 0|\t\tpass|	tests/unit/test_saves.gd	the v7 step leaves the party with no mp at all, so a migrated save is malformed
scripts/data/save_data.gd	s|if int\(party.get\(\"mp\", 0\)\) < 0:|if false:|	tests/unit/test_saves.gd	a hand-edited save carrying negative magic loads without a word
scripts/ui/battle_logic.gd	s|out.mp = clampi\(member_mp, 0, curve.max_mp\(out.level\)\)|out.mp = member_mp|	tests/unit/test_battle_logic.gd	a save claiming more magic than the curve allows fights with all of it
scripts/ui/battle_logic.gd	s|\t\t\twho.mp = who.max_mp\(\)|\t\t\tpass|	tests/unit/test_battle_logic.gd	levelling up restores the health and leaves the caster empty
scripts/ui/battle_logic.gd	s|"level": member.level, "mp": member.mp\}\)|"level": member.level})|	tests/unit/test_battle_logic.gd	a fight seals without saying what magic it left, so the sink writes nought over it
scripts/world/world_scene.gd	s|int\(record.get\("level", 1\)\), int\(record.get\("mp", 0\)\)\)$|int(record.get("level", 1)), 0)|	tests/integration/test_world_battles.gd	the one sink ignores the magic a fight reported and empties the player
scripts/world/world_scene.gd	s|GameState.player_level, GameState.player_mp,|GameState.player_level, 0,|	tests/integration/test_world_battles.gd	every fight opens with an empty caster, whatever the player was carrying
scripts/world/world_scene.gd	s|\t\t_game.combat.max_mp\(starting\)\)|\t\t0)|	tests/integration/test_world_battles.gd	a player who has never fought starts with no magic at all
scripts/world/world_scene.gd	s|\tGameState.set_party\(_game.combat.max_hp\(starting\)|\tGameState.set_party(1|	tests/integration/test_world_battles.gd	a player who has never fought starts on one hit point

# --- M25: magic in a fight ---
scripts/ui/battle_logic.gd	s|^\treturn row.cost <= _fighter\(_commander\).mp$|\treturn true|	tests/unit/test_battle_logic.gd	a spell beyond the player's magic is offered and cast anyway
scripts/ui/battle_logic.gd	s|\tif not can_afford\(row\):|\tif false:|	tests/unit/test_battle_logic.gd	casting past the purse goes through and the magic runs negative
scripts/ui/battle_logic.gd	s|^\tcaster.mp -= row.cost$|\tpass|	tests/fixtures/qa/quest/cast_a_spell_in_a_fight.json	every spell is free, so the magic is a number that never moves
scripts/ui/battle_logic.gd	s|\t\t\t_phase = Phase.SPELLS|\t\t\tpass|	tests/unit/test_battle_logic.gd	the Magic command opens nothing at all
scripts/ui/battle_logic.gd	s|sleeper.status.asleep_turns = row.status_turns|pass|	tests/unit/test_battle_logic.gd	a sleep spell says it landed and puts nothing to sleep
scripts/ui/battle_logic.gd	s|\t\t\thit.hp = maxi\(hit.hp - dealt, 0\)|\t\t\tpass|	tests/unit/test_battle_logic.gd	an attack spell announces damage and deals none
scripts/ui/battle_logic.gd	s|if it.status.asleep_turns > 0:|if false:|	tests/unit/test_battle_logic.gd	a sleeping enemy telegraphs and hits anyway
scripts/ui/battle_logic.gd	s|it.status.asleep_turns -= 1|pass|	tests/unit/test_battle_logic.gd	a sleep never ends, so one cast finishes the fight
scripts/ui/battle_logic.gd	s|\t\t_index = Row.MAGIC|\t\t_index = Row.ATTACK|	tests/unit/test_battle_logic.gd	backing out of the spells lands the cursor on Attack instead of Magic
scripts/ui/battle_logic.gd	s|if def == null or def.learn_level > level:|if def == null:|	tests/integration/test_world_battles.gd	every spell in the game is castable from level one
scripts/ui/battle_screen.gd	s|\treturn clampi\(_logic.index\(\) - VISIBLE_ROWS \+ 1, 0, count - VISIBLE_ROWS\)|\treturn 0|	tests/integration/test_battle_layout.gd	a page longer than the window never scrolls, so its last rows cannot be reached
scripts/ui/battle_screen.gd	s|\t\tvar at := first \+ i|\t\tvar at := i|	tests/integration/test_battle_layout.gd	the window is worked out and then ignored, so the rows drawn never move
scripts/ui/battle_screen.gd	s|\tlabel.horizontal_alignment = HORIZONTAL_ALIGNMENT_RIGHT|\tpass|	tests/integration/test_battle_layout.gd	the player block runs left across the screen and lands on the command list again
scripts/ui/battle_screen.gd	s|if _logic.member_count\(\) != 1 or _logic.member_max_mp\(0\) <= 0:|if false:|	tests/integration/test_battle_layout.gd	a game with no magic is told about magic it does not have
scripts/ui/battle_screen.gd	s|\t\treturn \"%s  %d MP\" % \[spell.name, spell.cost\]|\t\treturn spell.name|	tests/integration/test_world_battles.gd	the spell page names no prices, so the choice is made blind
scripts/world/world_scene.gd	s|\t\t\tout.append\("MP %d/%d" % \[mp, curve.max_mp\(level\)\]\)|\t\t\tpass|	tests/integration/test_world_equipment.gd	nothing outside a fight says how much magic is left

# --- M26: a battle theme and a fanfare ---
scripts/autoload/audio_bus.gd	s|\t\twav.loop_mode = AudioStreamWAV.LOOP_DISABLED|\t\tpass|	tests/unit/test_audio_bus.gd	a fanfare loops forever, so the room is never handed back
scripts/autoload/audio_bus.gd	s|\t_music_left = ceili\(once.get_length\(\) \* float\(Engine.physics_ticks_per_second\)\)|\t_music_left = 1|	tests/unit/test_audio_bus.gd	the theme comes back over the top of the fanfare instead of after it
scripts/autoload/audio_bus.gd	s|\t_music_left -= 1|\tpass|	tests/unit/test_audio_bus.gd	the chain never counts down, so a fanfare is the last thing the game plays
scripts/autoload/audio_bus.gd	s|\t\treturn play_or_silence\(then_id\)|\t\treturn false|	tests/unit/test_audio_bus.gd	a fanfare nobody rendered takes the map's theme down with it
scripts/autoload/audio_bus.gd	s|\t_music_left = -1|\tpass|	tests/unit/test_audio_bus.gd	the last fight's hand-back fires over the top of the next fight
scripts/world/world_scene.gd	s|\t\tAudioBus.play_music\(scored\)|\t\tpass|	tests/fixtures/qa/quest/hear_the_battle_out.json	the road's theme plays on through every fight
scripts/world/world_scene.gd	s|\t\t\t_leave_battle_music\(outcome == BattleLogic.Outcome.VICTORY\)|\t\t\tpass|	tests/fixtures/qa/quest/hear_the_battle_out.json	a fight ends and its theme keeps playing over the map it was fought in
scripts/world/world_scene.gd	s|\t\tAudioBus.play_music_then\(_game.victory_music, here\)|\t\tAudioBus.play_or_silence(here)|	tests/fixtures/qa/quest/hear_the_battle_out.json	a win is not stung at all, it just goes quiet
scripts/data/game_manifest.gd	s|\t\t\tout.append\(\"%s '%s' has no voice to play it in\" % \[field, tune\]\)|\t\t\tpass|	tests/unit/test_game_manifest.gd	a game names a tune and has nothing to play it with, and nothing says so
scripts/autoload/qa.gd	s|\t\t\t\tif \(live == tune\) != expect:|\t\t\t\tif false:|	tests/unit/test_qa_ops.gd	assert_music now passes whatever is playing, so a silence gate proves nothing

# --- M26.1: the title had no voice ---
scripts/world/world_scene.gd	s|\tAudioBus.use_style\(_offered.sound_style\)|\tpass|	tests/integration/test_world_music.gd	the title plays into a bus with no voice bound, and is silent on every platform
scripts/autoload/audio_bus.gd	s|\tif not _play\(_music, id, false\):|\tif false:|	tests/unit/test_audio_bus.gd	the bus reports a track it could not play as the one that is playing
scripts/autoload/qa.gd	s|\t\t\tif String\(AudioBus.style_id\(\)\).is_empty\(\):|\t\t\tif false:|	tests/unit/test_qa_ops.gd	assert_audio_ready passes green when there is no voice bound to ask about
scripts/data/party_member_def.gd	s|if String\(character\).is_empty\(\):|if false:|	tests/unit/test_party.gd	a party member with no art is accepted and fights invisibly
scripts/data/party_member_def.gd	s|if join_level < 1:|if false:|	tests/unit/test_party.gd	a member joins at level nought
scripts/data/party_member_def.gd	s|elif seen_spells.has\(spell_id\):|elif false:|	tests/unit/test_party.gd	a member lists one spell twice and nobody says so
scripts/data/game_manifest.gd	s|if not FileAccess.file_exists\(member_sheet\):|if false:|	tests/unit/test_game_manifest.gd	a party member whose art was never generated ships
scripts/data/game_manifest.gd	s|if member_ids.has\(member.id\):|if false:|	tests/unit/test_game_manifest.gd	two members under one id share one save record
scripts/data/game_manifest.gd	s|if not party.is_empty\(\) and combat == null:|if false:|	tests/unit/test_game_manifest.gd	a party is accepted with no curve for it to grow on
scripts/autoload/game_state.gd	s|return player_hp <= 0 and companions.is_empty\(\)|return player_hp <= 0|	tests/unit/test_party.gd	a leader who fell beside a companion is silently resurrected
scripts/autoload/game_state.gd	s|^\tif String\(id\).is_empty\(\):|\tif false:|	tests/unit/test_party.gd	the leader is written into the companion map under an empty id
scripts/autoload/game_state.gd	s|\tcompanions = \{\}$|\tpass|	tests/unit/test_party.gd	a new game starts with the last one companions
scripts/autoload/game_state.gd	s|\tvar already := 1 if worn.get\(slot, \&""\) == id else 0|\tvar already := 0|	tests/unit/test_party.gd	re-equipping what is already worn starts answering no
scripts/autoload/game_state.gd	s|\tmembers.reverse\(\)|\tpass|	tests/unit/test_party.gd	which companion loses a sold sword depends on the day
scripts/autoload/game_state.gd	s|\t\ttotal \+= worn.values\(\).count\(id\)|\t\tpass|	tests/unit/test_party.gd	what a companion wears is not counted as worn at all
scripts/data/save_data.gd	s|if int\(claimed\[worn_id\]\) > int\(items.get\(worn_id, 0\)\):|if false:|	tests/unit/test_saves.gd	a save with two people wearing one carried sword loads
scripts/data/save_data.gd	s|if not companions.is_empty\(\) and party.is_empty\(\):|if false:|	tests/unit/test_saves.gd	a companion joined a player who does not exist
scripts/data/migrations.gd	s|\td\["companions"\] = \{\}|\tpass|	tests/unit/test_saves.gd	the v8 to v9 step forgets the key it exists to add
scripts/autoload/qa.gd	s|if not GameState.has_companion\(member\):|if false:|	tests/unit/test_qa_ops.gd	a companion who never joined answers assertions about their health
scripts/autoload/qa.gd	s|\t\t\t_fail\("assert_gold names member '%s', and there is one purse" % member\)|\t\t\tpass|	tests/unit/test_qa_ops.gd	the harness invents a per-member purse
scripts/ui/battle_logic.gd	s|^\t\tif who > acted:$|\t\tif false:|	tests/unit/test_battle_logic.gd	the turn never reaches the second member
scripts/ui/battle_logic.gd	s|^\tvar acted := _commander$|\tvar acted := -1|	tests/unit/test_battle_logic.gd	the first member takes every turn and the enemy never acts
scripts/ui/battle_logic.gd	s|^\t_commander = -1$|\tpass|	tests/integration/test_battle_layout.gd	a member stays marked while the enemy is taking aim
scripts/ui/battle_logic.gd	s|^\tif standing.size\(\) < 2:$|\tif false:|	tests/unit/test_battle_logic.gd	a solo heal opens a cursor with one row on it
scripts/ui/battle_logic.gd	s|\tif row.kind == SpellDef.Kind.HEAL or row.kind == SpellDef.Kind.BOOST:|\tif false:|	tests/unit/test_battle_logic.gd	a heal in a party lands on whoever cast it
scripts/ui/battle_logic.gd	s|\tchosen.target = standing\[_index\]|\tchosen.target = _commander|	tests/unit/test_battle_logic.gd	the ally cursor is drawn, read and ignored
scripts/ui/battle_logic.gd	s|^\t\tif _living\(\).is_empty\(\):$|\t\tif true:|	tests/unit/test_battle_logic.gd	one member falling loses the whole fight
scripts/ui/battle_logic.gd	s|^\tfor i in _living\(\):$|\tfor i in _members.size():|	tests/unit/test_battle_logic.gd	a member who was down through the fight earns experience
scripts/ui/battle_logic.gd	s|\t_target = standing\[_target_rng.next_int\(0, standing.size\(\) - 1\)\]|\t_target = 0|	tests/unit/test_battle_logic.gd	the enemy always swings at whoever is first in the party
scripts/ui/battle_logic.gd	s|\treturn _fighter\(_commander\).spells.duplicate\(\)|\treturn _fighter(0).spells.duplicate()|	tests/unit/test_battle_logic.gd	every member is offered the first member's spells
scripts/world/world_scene.gd	s|if String\(member.joins_on_flag\).is_empty\(\) or GameState.has_flag\(member.joins_on_flag\):|if true:|	tests/integration/test_world_party.gd	everybody on the roster is in the party from the start
scripts/ui/battle_logic.gd	s|^\t\t\tif not everything and not only.has\(def.id\):$|\t\t\tif false:|	tests/integration/test_world_party.gd	a companion knows every spell in the game
scripts/world/world_scene.gd	s|GameState.set_companion\(member.id, curve.max_hp\(level\), earned, level, curve.max_mp\(level\)\)|GameState.set_companion(member.id, 1, 0, 1, 0)|	tests/integration/test_world_party.gd	a companion joins at one hit point whatever their curve says
scripts/world/world_scene.gd	s|\t\t\t\t\t\tGameState.set_companion\(member, int\(record.get\("hp", 0\)\),|\t\t\t\t\t\tGameState.set_party(int(record.get("hp", 0)),|	tests/integration/test_world_party.gd	a companion's fight result is written over the leader
scripts/ui/battle_screen.gd	s|\t\tvar mine := acting and player_side and swinging == i|\t\tvar mine := acting and player_side|	tests/integration/test_battle_layout.gd	every member leans when any one of them swings
scripts/ui/battle_screen.gd	s|\treturn MEMBERS_Y \+ at \* MEMBER_PITCH|\treturn MEMBERS_Y|	tests/integration/test_battle_layout.gd	a party's blocks are all drawn on top of each other

scripts/autoload/game_state.gd	s|^\tif String\(slot\).is_empty\(\):$|\tif false:|	tests/unit/test_party.gd	gear can be equipped into a slot that is not a slot
scripts/ui/pause_menu.gd	s|^\tif _members.size\(\) > 1:$|\tif false:|	tests/unit/test_pause_menu.gd	a party opens equipment without asking whose
scripts/ui/pause_menu.gd	s|\t\t_member = clampi\(_index, 0, _members.size\(\) - 1\)|\t\t_member = 0|	tests/unit/test_pause_menu.gd	the member page is drawn, read and ignored
scripts/ui/pause_menu.gd	s|\tif has_members\(\) and \(_page == Page.EQUIP or _page == Page.STATUS\):|\tif false:|	tests/unit/test_pause_menu.gd	backing out of a member's page leaves the menu entirely
scripts/world/world_scene.gd	s|\treturn \[\] if out.size\(\) < 2 else out|\treturn out|	tests/integration/test_world_party.gd	a solo game is asked which member it means
scripts/world/world_scene.gd	s|\t_pause_member = member|\tpass|	tests/integration/test_world_party.gd	picking a member changes whose page is drawn not at all
scripts/world/world_scene.gd	s|\t\tGameState.set_companion\(member.id, curve.max_hp\(at\), int\(numbers.get\("xp", 0\)\), at,|\t\tGameState.set_companion(member.id, 1, int(numbers.get("xp", 0)), at,|	tests/integration/test_world_party.gd	a night's sleep leaves the companion where they fell
scripts/world/world_scene.gd	s|^\tfor joined in _active_party\(\):$|\tfor joined in []:|	tests/fixtures/qa/quest/recruit_a_companion.json	somebody joins and has no numbers until the next fight asks for them
scripts/ui/battle_logic.gd	s|^\tif alive.size\(\) < 2:$|\tif true:|	tests/unit/test_battle_logic.gd	a formation is swung at without ever asking which one
scripts/ui/battle_logic.gd	s|^\t\tif next_up > taken:$|\t\tif false:|	tests/unit/test_battle_logic.gd	only the first foe in a formation ever takes a turn
scripts/ui/battle_logic.gd	s|\t\tearned \+= _foe\(at\).def.xp|\t\tpass|	tests/unit/test_battle_logic.gd	a formation pays the experience of one of its foes
scripts/ui/battle_logic.gd	s|\t\tpurse \+= _foe\(at\).def.gold|\t\tpass|	tests/unit/test_battle_logic.gd	a formation pays the coin of one of its foes
scripts/ui/battle_logic.gd	s|\t\t\t\teach.hp = maxi\(each.hp - took, 0\)|\t\t\t\tpass|	tests/unit/test_battle_logic.gd	a spell that reaches everything announces damage and deals none
scripts/ui/battle_logic.gd	s|\t\t\tdisplay = "%s %s" % \[entry.name, char\("A".unicode_at\(0\) \+ nth\)\]|\t\t\tpass|	tests/unit/test_battle_logic.gd	two foes of one kind are both called the same thing
scripts/world/map_data.gd	s|^\t\tif occupied.has\(stands\):$|\t\tif false:|	tests/unit/test_map_data.gd	two enemies stand on one tile and the second can never be fought
scripts/ui/battle_screen.gd	s|\t\tvar theirs := acting and not player_side and swinging_foe == at|\t\tvar theirs := acting and not player_side|	tests/integration/test_battle_layout.gd	every foe leans when any one of them swings
scripts/ui/battle_screen.gd	s|^const MAX_FOES := 3$|const MAX_FOES := 0|	tests/unit/test_battle_content.gd	a map may name a formation bigger than the screen draws
scripts/ui/battle_screen.gd	s|^const MAX_PARTY := 3$|const MAX_PARTY := 0|	tests/unit/test_battle_content.gd	a game may declare a party bigger than the screen draws
scripts/ui/battle_logic.gd	s|\t\t\t\tdef.status_turns, def.target, def.stat, def.element\)\)|\t\t\t\tdef.status_turns, def.stat, def.element))|	tests/integration/test_world_battles.gd	a spell's shape is dropped between the file and the fight
scripts/ui/battle_logic.gd	s|who.status.attack_bonus\(\)|0|	tests/unit/test_battle_logic.gd	a boost never reaches the swing, so the spell announces itself and changes no number
scripts/ui/battle_logic.gd	s|it.status.defense_bonus\(\)|0|	tests/unit/test_battle_logic.gd	a sap never reaches the guard the blow is measured against
scripts/ui/battle_logic.gd	s|maxi\(0, it.def.defense \+ it.status.defense_bonus\(\)\)|it.def.defense + it.status.defense_bonus()|	tests/unit/test_battle_logic.gd	an over-sapped guard goes negative and starts helping the attacker
scripts/ui/battle_logic.gd	s|if member.status.asleep_turns > 0:|if false:|	tests/unit/test_battle_logic.gd	a sleeping party member is handed the menu anyway, which is a free turn
scripts/ui/battle_logic.gd	s|if pressed_in_time\(\):|if false:|	tests/unit/test_battle_logic.gd	a well-timed guard stops shrugging off an affliction, so the cue goes quiet in status fights
scripts/ui/battle_logic.gd	s|def.target, def.stat, def.element\)\)|def.target, def.element))|	tests/integration/test_world_battles.gd	a spell's stat is dropped between the file and the fight, so every boost moves attack
data/spells/ward.tres	s|status_turns = 3|status_turns = 0|	tests/unit/test_battle_content.gd	the game's one buff lasts no turns, so it announces itself and changes nothing
data/enemies/gloom.tres	s|"turns": 3|"turns": 0|	tests/unit/test_battle_content.gd	the game's one affliction lasts no turns
data/spells/ward.tres	s|learn_level = 2|learn_level = 9|	tests/fixtures/qa/quest/a_ward_against_the_chill.json	the buff is learned past the level cap, so no played fight can reach it
# --- M31: the flow model, composed ---
scripts/world/world_scene.gd	s|\t_pause.queue_free\(\)|\tpass|	tests/integration/test_flow_model.gd	a closed pause screen is never freed, so it eats the cancel key and the menu never opens a second time
scripts/world/world_scene.gd	s|\t_shop.queue_free\(\)|\tpass|	tests/integration/test_flow_model.gd	a closed shop screen is never freed, so the second visit to a counter opens nothing at all
# --- M32: the game over and the boss have their own music ---
scripts/world/world_scene.gd	s|AudioBus.play_or_silence\(_game.game_over_music\)|AudioBus.stop_music()|	tests/integration/test_world_music.gd	a defeat cuts the music dead again, so a game that names a game-over theme never plays it
scripts/world/world_scene.gd	s|\t\t\treturn entry.music|\t\t\treturn &\"\"|	tests/integration/test_world_music.gd	an enemy's own theme stops outranking the manifest's, so every boss fight sounds like an ordinary one
data/games/quest.tres	s|game_over_music = &\"dirge\"|game_over_music = &\"\"|	tests/fixtures/qa/quest/fall_at_the_keep.json	the shipped game loses its game-over theme, and the only session that reaches one hears nothing
data/enemies/keeper.tres	s|music = &\"vigil\"|music = &\"\"|	tests/fixtures/qa/quest/fall_at_the_keep.json	the boss gives up his own theme, so the fight the demo is built around sounds like the ones in the hollow
# --- M32: a slot says why it cannot be loaded ---
scripts/autoload/save_manager.gd	s|return SlotSummary.broken\(\) if glance.exists else SlotSummary.empty\(\)|return SlotSummary.empty()|	tests/unit/test_saves.gd	a file that is there and cannot be read is reported as an empty slot again
scripts/ui/pause_menu.gd	s|\t\treturn "Slot %d: damaged" % \(at \+ 1\)|\t\treturn "Slot %d: empty" % (at + 1)|	tests/unit/test_pause_menu.gd	a damaged slot draws as empty, which is the wording that invites saving over it
scripts/ui/slot_menu.gd	s|if summary != null and summary.has_save\(\):|if summary != null:|	tests/unit/test_title_menu.gd	every slot counts as a save, so a title with nothing saved offers Continue
scripts/ui/battle_logic.gd	s|var pct := target.def.resistance_to\(row.element\)|var pct := 100|	tests/unit/test_battle_logic.gd	a resistance is never read, so every element lands at face value
scripts/ui/battle_logic.gd	s|return maxi\(row.power \* pct / 100, 1\)|return row.power * pct / 100|	tests/unit/test_battle_logic.gd	a resisted spell rounds down to nothing, so resisted and immune stop differing
scripts/data/spell_def.gd	s|if kind != Kind.ATTACK and not String\(element\).is_empty\(\):|if false:|	tests/unit/test_spell_def.gd	a heal or a sleep ships made of fire, with no damage for it to scale
scripts/data/enemy_def.gd	s|\tout.append_array\(_resistance_problems\(\)\)|\tpass|	tests/unit/test_enemy_def.gd	the resistance map is never validated at all
scripts/data/enemy_def.gd	s|\t\telif int\(pct\) == 100:|\t\telif false:|	tests/unit/test_enemy_def.gd	an answer of exactly 100% ships, reading like a decision and changing nothing
scripts/data/enemy_def.gd	s|\t\tif int\(pct\) < 0:|\t\tif false:|	tests/unit/test_enemy_def.gd	an enemy ships healed by an element instead of hurt by it
scripts/ui/battle_logic.gd	s|def.status_turns, def.target, def.stat, def.element\)\)|def.status_turns, def.target, def.stat))|	tests/integration/test_world_battles.gd	a spell's element is dropped between the file and the fight, so every spell lands at face value
data/enemies/gloom.tres	s|&"fire": 200|&"fire": 150|	tests/fixtures/qa/quest/fire_finds_the_gloom.json	the Gloom's weakness to fire is retuned and the played session does not notice
scripts/ui/battle_logic.gd	s|\tif answer > 100:|\tif false:|	tests/unit/test_battle_logic.gd	a weakness lands silently, so a bigger number arrives with nothing to compare it against
scripts/ui/battle_logic.gd	s|\tif answer < 100:|\tif false:|	tests/unit/test_battle_logic.gd	a resistance lands silently, and a small number reads as a weak spell
tests/helpers/battle_driver.gd	s|\t\t\t\t\t\t_aim\(logic, _toughest_row\(logic\)\)|\t\t\t\t\t\t_aim(logic, _weakest_row(logic))|	tests/unit/test_battle_content.gd	the caster aims like PERFECT, so a boss is never the target of a spell
tests/helpers/battle_helpers.gd	s|\t\tBattleLogic.SpellRow.page\(defs, level, \[\], true\)\)\)|\t\t[]))|	tests/unit/test_battle_content.gd	the balance party knows no magic, which is what it knew before M34
scripts/ui/battle_logic.gd	s|\t\t\tif def == null or def.battle_heal <= 0:|\t\t\tif def == null:|	tests/unit/test_battle_content.gd	a quest item reaches the battle menu, where using it destroys it
tests/helpers/battle_driver.gd	s|\treturn rows\[used_count % rows.size\(\)\]|\treturn rows[0]|	tests/unit/test_battle_driver.gd	the drinker always reaches for the first row, exercising one item
tests/helpers/battle_driver.gd	s|\t\tif logic.member_hp\(at\) > 0 and logic.member_hp\(at\) < logic.member_max_hp\(at\):|\t\tif true:|	tests/unit/test_battle_driver.gd	the drinker drinks at full health, so it never swings
tests/helpers/battle_driver.gd	s|\tif logic.item_rows\(\).is_empty\(\):|\tif false:|	tests/unit/test_battle_driver.gd	an empty bag is opened anyway, and a dead row is pressed forever
tests/helpers/battle_driver.gd	s|\tif policy != Policy.DRINKER:|\tif false:|	tests/unit/test_battle_driver.gd	every policy is offered the bag, so no report is about its own verb
tests/helpers/battle_driver.gd	s|\t\tif logic.enemy_hp\(rows\[i\]\) > logic.enemy_hp\(rows\[best\]\):|\t\tif logic.enemy_hp(rows[i]) < logic.enemy_hp(rows[best]):|	tests/unit/test_battle_driver.gd	both aiming policies point the same way, so the aim axis does not exist
tests/helpers/battle_driver.gd	s|\tif _will_drink\(logic, policy\):|\tif false:|	tests/unit/test_battle_content.gd	the drinker never opens the bag, so no item is ever played
data/enemies/keeper.tres	s|boss = true|boss = false|	tests/unit/test_battle_content.gd	the game's one mandatory fight becomes optional, and can be walked away from
data/enemies/slink.tres	s|boss = false|boss = true|	tests/unit/test_battle_content.gd	the tutorial fight becomes inescapable
tests/helpers/battle_driver.gd	s|\t\treturn BattleLogic.Row.FLEE|\t\treturn BattleLogic.Row.ATTACK|	tests/unit/test_battle_content.gd	the runner never presses Run, so no escape is ever attempted
scripts/ui/battle_screen.gd	s|\t_message.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART|\t_message.autowrap_mode = TextServer.AUTOWRAP_OFF|	tests/integration/test_battle_layout.gd	the caption stops wrapping, so a long line is drawn past the window edge
scripts/ui/battle_screen.gd	s|\t_message.size.x = float\(viewport_size.x\) - MARGIN \* 2.0|\t_message.size.x = 1.0|	tests/integration/test_battle_layout.gd	the caption wraps against one pixel, which is what a Label falls back to
scripts/ui/battle_logic.gd	s|\tif not _queued.is_empty\(\):|\tif false:|	tests/unit/test_battle_logic.gd	the queue is never drained, so a sweep reports only its first foe
scripts/ui/battle_logic.gd	s|\t_queued = lines.slice\(1\)|\t_queued = []|	tests/unit/test_battle_logic.gd	only the first line of a sequence is queued, and the rest are lost
scripts/ui/battle_logic.gd	s|\t\t_say\(next_line, _after_message\)|\t\t_say(next_line, Phase.PLAYER_ACT)|	tests/unit/test_battle_logic.gd	a sequence forgets where it was going, so a winning sweep hands the turn back
scripts/ui/battle_logic.gd	s|\t\t\t\tlines.append\(_award_victory\(" and ".join\(felled\)\)\)|\t\t\t\tpass|	tests/unit/test_battle_logic.gd	a sweep that wins the fight never says so
scripts/ui/battle_logic.gd	s|\t\treturn " %s is weak to %s\." % \[subject, row.element\]|\t\treturn ""|	tests/unit/test_battle_logic.gd	a weakness lands silently, with nothing to compare the bigger number against
scripts/ui/battle_logic.gd	s|\t\treturn " %s shrugs off most of the %s\." % \[subject, row.element\]|\t\treturn ""|	tests/unit/test_battle_logic.gd	a resistance lands silently, and a small number reads as a weak spell
scripts/ui/battle_logic.gd	s|var frame := "%s casts %s\." % \[caster.name, row.name\]|var frame := ""|	tests/unit/test_battle_logic.gd	the persistent frame is dropped, so a sequence says who is acting nowhere
scripts/util/tiled_map.gd	s|\tif count != tile_ids.size\(\):|\tif false:|	tests/unit/test_tiled_map.gd	a map painted against a different-sized bank is accepted, so every id past the change is wrong
scripts/util/tiled_map.gd	s|\tif named != String\(style\):|\tif false:|	tests/unit/test_tiled_map.gd	a map painted for another style is accepted, and every tile on it becomes a different tile
scripts/util/tiled_map.gd	s|\t\t\tvar at := gid - first_gid|\t\t\tvar at := gid|	tests/unit/test_tiled_map.gd	the tileset's firstgid is ignored, so every tile on an imported map is off by one
scripts/util/tiled_map.gd	s|\t\t\t\tvalue = JSON_PREFIX \+ JSON.stringify\(value\)|\t\t\t\tvalue = str(value)|	tests/unit/test_tiled_map.gd	a patrol path or a formation is written as text and cannot come back as data
scripts/util/tiled_map.gd	s|\tif bool\(raw.get\("infinite", false\)\):|\tif false:|	tests/unit/test_tiled_map.gd	an infinite Tiled map is accepted, and a template map has a fixed size
scripts/util/tiled_map.gd	s|\tif sets.size\(\) != 1:|\tif false:|	tests/unit/test_tiled_map.gd	a map painted from two banks is accepted, and only one of them can be resolved
scripts/data/game_config.gd	s|if not SAVE_POLICIES.has\(save_policy\):|if false:|	tests/unit/test_game_config.gd	a save policy nobody implements loads quietly, so the Save row and the save point disagree
scripts/ui/pause_menu.gd	s|if row == Row.SAVE and not _can_save:|if false:|	tests/unit/test_pause_menu.gd	a save-at-point game still offers the Save row, and the menu writes what the design forbade
scripts/ui/pause_menu.gd	s|\tvar row := top_row\(_index\)|\tvar row := _index|	tests/unit/test_pause_menu.gd	the cursor is read as a Row, so a hidden row makes every press below it answer for its neighbour
scripts/ui/pause_screen.gd	s|\t\t\tvar row := _menu.top_row\(at\)|\t\t\tvar row := at|	tests/integration/test_pause_and_saves.gd	the menu labels by the raw index, drawing Save over the row that answers Load
scripts/ui/save_menu.gd	s|\tif _slots.is_empty\(\):|\tif _slots.is_empty() or summary(_index) == null or not summary(_index).has_save():|	tests/unit/test_save_menu.gd	a save point refuses an empty slot, so a new game can never write its first save
scripts/world/world_scene.gd	s|\treturn _config == null or _config.save_policy != GameConfig.SAVE_AT_POINT|\treturn true|	tests/integration/test_pause_and_saves.gd	the world stops reading the save policy and every game saves from the menu
scripts/world/world_scene.gd	s|\t\t\t\topen_save.call_deferred\(\)|\t\t\t\topen_save()|	tests/integration/test_pause_and_saves.gd	a save point opens inline and the dialog's own close pops it straight back off
scripts/world/world_scene.gd	s|\t_saving.refresh\(_slot_summaries\(\)\)|\tpass|	tests/integration/test_pause_and_saves.gd	a written slot still reads as empty, so the save looks like a press the game swallowed
scripts/world/world_scene.gd	s|\tRouter.open_overlay\(Router.State.SAVING\)|\tRouter.open_overlay(Router.State.PAUSED)|	tests/integration/test_flow_model.gd	a save point announces itself as the pause menu and the flow model stops describing the machine
scripts/ui/save_screen.gd	s|\t\tleft.emit\(\)|\t\tpass|	tests/integration/test_pause_and_saves.gd	a save point cannot be left, so cancel is a dead key over a screen with no other way out
scripts/ui/dialog_runner.gd	s|if bool\(choice.get\("open_save", false\)\):|if false:|	tests/unit/test_dialog_runner.gd	a choice carrying open_save collects nothing and the priest is a conversation that does nothing
scripts/data/game_config.gd	s|const MAX_SAVE_SLOTS := 12|const MAX_SAVE_SLOTS := 16|	tests/integration/test_slot_layout.gd	the declared slot capacity grows past what either screen can draw, and the last rows go off the bottom
scripts/data/game_config.gd	s|if save_slots > MAX_SAVE_SLOTS:|if false:|	tests/integration/test_slot_layout.gd	a game may ship more save slots than a slot list can draw
scripts/ui/save_screen.gd	s|const ROW_PITCH := 10|const ROW_PITCH := 0|	tests/integration/test_slot_layout.gd	every slot row is drawn on the same line, which is inside the window and unreadable
scripts/ui/save_screen.gd	s|\tvar count := maxi\(_menu.slot_count\(\), 1\)|\tvar count := 1|	tests/integration/test_slot_layout.gd	the save point draws one row whatever the game offers, so every slot but the first is unreachable
.github/workflows/ci.yml	s|github.event.pull_request.number|github.ref|	tests/unit/test_ci_paths.gd	the gate's lane goes back to the ref, so the next merge to main cancels this one's sweep and skips its deploy
.github/workflows/ci.yml	s|github.event.pull_request.number|github.sha|	tests/unit/test_ci_paths.gd	two pushes to one branch stop superseding each other and a stale run gates the merge
.github/workflows/ci.yml	s|cancel-in-progress: true|cancel-in-progress: false|	tests/unit/test_ci_paths.gd	nothing supersedes anything, so every fix-up commit leaves a run nobody is waiting for
scripts/ui/dialog_runner.gd	s|if not shop.is_empty\(\):|if false:|	tests/unit/test_dialog_runner.gd	a choice carrying open_shop collects nothing, so the keeper's counter never opens
scripts/ui/dialog_runner.gd	s|"shop": StringName\(shop\)|"shop": StringName("")|	tests/unit/test_dialog_runner.gd	the counter opens with no shop named, which reads as a broken menu rather than a typo
scripts/world/map_data.gd	s|if before.ground_at\(at\) != after.ground_at\(at\):|if false:|	tests/unit/test_map_data.gd	a converted map may come back with different tiles on it and every round-trip gate still passes
scripts/world/map_data.gd	s|if was != now:|if false:|	tests/unit/test_map_data.gd	an npc, a warp or a whole formation is lost in translation and nothing reports it
scripts/world/map_data.gd	s|if before.size\(\) != after.size\(\):|if false:|	tests/unit/test_map_data.gd	a map that came back a different size is compared cell by cell against one that is not there
scripts/world/map_data.gd	s|return int\(value\) if is_equal_approx\(value, roundf\(value\)\) else value|return value|	tests/unit/test_map_data.gd	5.0 and 5 read as different coordinates, so every trip through a file reports every record changed
scripts/util/tiled_map.gd	s|return str\(_read_properties\(raw.get\("properties", \[\]\)\).get\("style", ""\)\)|return "gb16"|	tests/unit/test_tiled_map.gd	a map whose bank is unnamed is guessed at, which is the silent wrong-tiles failure problems() exists to refuse
scripts/util/ldtk_map.gd	s|if across \* down != tile_ids.size\(\):|if false:|	tests/unit/test_ldtk_map.gd	the bank changed under an LDtk map and every tile past the change is silently a different tile
scripts/util/ldtk_map.gd	s|if named != String\(style\):|if false:|	tests/unit/test_ldtk_map.gd	an LDtk map painted for one style is read as another and every cell resolves to the wrong tile
scripts/util/ldtk_map.gd	s|if bool\(raw.get\("externalLevels", false\)\):|if false:|	tests/unit/test_ldtk_map.gd	a project keeping its levels in separate files reads as a map with nothing on it
scripts/util/ldtk_map.gd	s|if levels.size\(\) != 1:|if false:|	tests/unit/test_ldtk_map.gd	a multi-level project is imported as whichever level happened to be first
scripts/util/ldtk_map.gd	s|"src": \[\(at % across\) \* tile_size, \(at / across\) \* tile_size\],|"src": [0, 0],|	tests/unit/test_ldtk_map.gd	every tile points at the atlas origin, so the map round-trips here and draws as one tile in the editor
scripts/util/ldtk_map.gd	s|"px": \[x \* tile_size, y \* tile_size\],|"px": [x, y],|	tests/unit/test_ldtk_map.gd	tile positions are written in cells where LDtk reads pixels, so the whole map draws in one corner
scripts/util/ldtk_map.gd	s|\t\t\t\tout\[name\] = "String"|\t\t\t\tpass|	tests/unit/test_ldtk_map.gd	a field the records disagree about keeps the first type seen, and every other value is rewritten
scripts/util/ldtk_map.gd	s|var digest := label.sha256_text\(\)|var digest := "0".repeat(64)|	tests/unit/test_ldtk_map.gd	every iid in the file is the same one, which LDtk accepts and which silently collapses entity references
scripts/util/ldtk_map.gd	s|"gridTiles": \[\], "entityInstances": \[\],|"gridTiles": [],|	tests/unit/test_ldtk_map.gd	a layer ships without the entityInstances array LDtk reads unguarded, so the file will not open at all
scripts/util/tiled_map.gd	s|\treturn "tiles_%s.png" % style|\treturn "tiles.png"|	tests/unit/test_tiled_map.gd	the atlas loses its style name, so two banks collide on one file and half the maps wear the wrong art
scripts/util/ldtk_map.gd	s|\treturn "tiles_%s.png" % style|\treturn "tiles.png"|	tests/unit/test_ldtk_map.gd	the LDtk atlas name stops matching Tiled's, so one export directory serves neither editor
scripts/util/ldtk_map.gd	s|\tout\["__tilesetRelPath"\] = atlas_name\(style\)|\tout["__tilesetRelPath"] = null|	tests/unit/test_ldtk_map.gd	a tile layer names no sheet, and LDtk draws the map with nothing on it
# M40 - LPC import: the converter, the style axis, and the split between rig gates and import gates.
scripts/spritegen/lpc_import.gd	s|const LPC_ROW_ORDER: Array\[int\] = \[Dir.D.UP, Dir.D.LEFT, Dir.D.DOWN, Dir.D.RIGHT\]|const LPC_ROW_ORDER: Array[int] = [Dir.D.UP, Dir.D.RIGHT, Dir.D.DOWN, Dir.D.LEFT]|	tests/unit/test_lpc_import.gd	LPC's left and right rows are swapped, so an imported character walking west faces east
scripts/spritegen/lpc_import.gd	s|const WALK_CYCLE: Array\[int\] = \[1, 2, 3, 4, 5, 6, 7, 8\]|const WALK_CYCLE: Array[int] = [0, 1, 2, 3, 4, 5, 6, 7]|	tests/unit/test_lpc_import.gd	the standing pose leaks into the walk cycle and the last stride is dropped
scripts/spritegen/lpc_import.gd	s|var lpc_row := WALK_ROW \+ LPC_ROW_ORDER.find\(Dir.ALL\[row\]\)|var lpc_row := WALK_ROW + row|	tests/unit/test_lpc_import.gd	the walk block is copied in LPC's row order rather than re-cut into canonical rows
scripts/spritegen/lpc_import.gd	s|if image.get_height\(\) < needed_tall:|if false:|	tests/unit/test_lpc_import.gd	a sheet too short for the walk rows is accepted and read past its end
scripts/spritegen/lpc_import.gd	s|if not allowed:|if false:|	tests/unit/test_lpc_import.gd	a layer whose artist offered no accepted licence is imported anyway
scripts/spritegen/lpc_import.gd	s|return style.licenses.has\(license_family\(raw\)\)|return style.licenses.any(func(f: String) -> bool: return raw.begins_with(f))|	tests/unit/test_lpc_import.gd	licences are prefix-matched, so a credit-only style waves share-alike through
scripts/spritegen/lpc_import.gd	s|ground = maxi\(ground, SpriteCompositor.ground_row\(src.get_region\(from\)\)\)|ground = FRAME - 1|	tests/unit/test_lpc_import.gd	the anchor is declared at the frame's bottom instead of measured from the feet
scripts/spritegen/lpc_import.gd	s|files.sort\(\)|pass|	tests/unit/test_lpc_import.gd	a character's credits come out in whatever order the browser walked the selection
scripts/spritegen/lpc_import.gd	s|names.sort\(\)|pass|	tests/unit/test_lpc_import.gd	the cast's credits come out in whatever order the characters were imported
scripts/data/sprite_style.gd	s|if not SHEET_SOURCES.has\(sheets_from\):|if false:|	tests/unit/test_rig_and_style.gd	a typo'd sheets_from reads as the rig arm rather than failing the build
scripts/data/sprite_style.gd	s|if not String\(rig_id\).is_empty\(\):|if false:|	tests/unit/test_rig_and_style.gd	an imported style may name a rig, so two arms answer for one style
scripts/data/sprite_style.gd	s|if licenses.is_empty\(\):|if false:|	tests/unit/test_rig_and_style.gd	an imported style with no licence list is accepted, and the gate has nothing to check against
tests/helpers/art_fixtures.gd	s|if not style\(style_id\).imports\(\):|if true:|	tests/unit/test_gates_consistency.gd	an imported style is handed to the rig gates, which have no rig to draw it with
scripts/ui/sprite_lab.gd	s|_refresh_imported\(\)$|pass|	tests/integration/test_sprite_lab_scene.gd	an imported style shows nothing in the lab, so the first hand-drawn character is judged blind
# M40 A.2 - LpcCompose: a recipe against the catalogue, the way the browser composes.
scripts/spritegen/lpc_compose.gd	s|const TOLERANCE := 1|const TOLERANCE := 2|	tests/unit/test_lpc_compose.gd	the recolour matches two units off, painting colours the artist never keyed
scripts/spritegen/lpc_compose.gd	s|return int\(a\[\"z\"\]\) < int\(b\[\"z\"\]\)|return int(a[\"order\"]) < int(b[\"order\"])|	tests/unit/test_lpc_compose.gd	layers draw in recipe order, so hair behind the body lands in front of the shirt
scripts/spritegen/lpc_compose.gd	s|var t: Color = to\[i\]|var t: Color = to[(i + 1) % to.size()]|	tests/unit/test_lpc_compose.gd	the remap is off by one tone, so every shadow becomes a highlight
scripts/spritegen/lpc_compose.gd	s|Vector2i\(0, LpcImport.WALK_ROW \* FRAME\)|Vector2i(0, 0)|	tests/unit/test_lpc_compose.gd	the walk block lands at the top of the sheet, where the importer never looks
scripts/spritegen/lpc_compose.gd	s|return matching if not matching.is_empty\(\) else all|return all|	tests/unit/test_lpc_compose.gd	every file of a definition is credited, including art the character does not wear
scripts/spritegen/lpc_compose.gd	s|if base_path.is_empty\(\):|if false:|	tests/unit/test_lpc_compose.gd	a layer with no art for the body type is planned anyway, drawing nothing in silence
scripts/spritegen/lpc_compose.gd	s|if not _covers_walk\(d\):|if false:|	tests/unit/test_lpc_compose.gd	a definition without a walk cycle is accepted, and the character walks headless
scripts/spritegen/lpc_compose.gd	s|if not allowed:|if false:|	tests/unit/test_lpc_compose.gd	the composer ignores the style's licences, leaving the importer as the only gate
scripts/data/sprite_style.gd	s|if world_scale < 1:|if false:|	tests/unit/test_rig_and_style.gd	a style drawn at no scale at all is accepted, and the world it opens has no size
scripts/util/ui_scale.gd	s|const DESIGN_SIZE := Vector2i\(320, 180\)|const DESIGN_SIZE := Vector2i(320, 176)|	tests/unit/test_ui_scale.gd	the size every screen is laid out for drifts from the one the game boots at
scripts/util/ui_scale.gd	s|window.content_scale_size = window_size\(style\)|pass|	tests/unit/test_ui_scale.gd	binding a 32px style leaves the world ten tiles wide
scripts/util/ui_scale.gd	s|layer.scale = layer_scale\(style\)|pass|	tests/integration/test_world_scale.gd	a screen is mounted at 1x over a 2x world, a quarter-size menu in the corner
scripts/util/ui_scale.gd	s|layer.scale = wanted|layer.scale = Vector2.ONE|	tests/integration/test_world_scale.gd	the dialog box and the hint, built before a style was known, are never brought up to it
scripts/world/world_scene.gd	s|UiScale.apply\(get_window\(\), _style\)|pass|	tests/integration/test_world_scale.gd	a style is bound and the window never follows it
scripts/world/world_scene.gd	s|_mount_ui\(_pause\)|add_child(_pause)|	tests/integration/test_world_scale.gd	one screen joins the tree around the mounting helper and draws at the wrong size
scripts/world/world_scene.gd	s|GameState.tile_size = _built.tile_size|pass|	tests/integration/test_world_scale.gd	the state never learns the map's tile size, so a save at 32px is written at 16
scripts/world/world_scene.gd	s|GameState.set_player\(_player.global_position, data.facing\)|pass|	tests/integration/test_world_scale.gd	after a load the state says the player stands somewhere the body does not
scripts/world/world_scene.gd	s|var at := at_tile \* float\(_built.tile_size\)|var at := at_tile|	tests/integration/test_world_scale.gd	a restored place is read as pixels, dropping the player in the corner of the map
scripts/data/migrations.gd	s|d = _v9_to_v10\(d\)|break|	tests/unit/test_saves.gd	a v9 save is handed on unmigrated, still speaking in pixels
scripts/data/migrations.gd	s|const PRE_V10_TILE_PIXELS := 16.0|const PRE_V10_TILE_PIXELS := 32.0|	tests/unit/test_saves.gd	every save written before v10 lands the player half way to where they stood
scripts/autoload/game_state.gd	s|out.tile = player_position / float\(maxi\(tile_size, 1\)\)|out.tile = player_position|	tests/unit/test_saves.gd	a save is written in pixels wearing a tile's name
scripts/ui/battle_screen.gd	s|var drawn := SPRITE_SCALE / float\(UiScale.scale_of\(_style\)\)|var drawn := SPRITE_SCALE|	tests/integration/test_battle_layout.gd	a fighter under a 32px style is drawn at four times its world size
scripts/world/npc_brain.gd	s|return ARRIVE_EPSILON_PER_TILE \* float\(_tile_size\)|return ARRIVE_EPSILON_PER_TILE * 16.0|	tests/unit/test_npc_brain.gd	at 32px tiles an NPC shuffles forever a pixel short of its waypoint
scripts/autoload/qa.gd	s|var tile_size := GameState.tile_size|var tile_size := 16|	tests/unit/test_qa_ops.gd	a play session reads a 32px world in 16px tiles and asserts the wrong door
tests/helpers/warp_graph.gd	s|changed = true|pass|	tests/unit/test_map_content.gd	the reach walk stops one door in, so the rooms beyond it are never compared with the rest
scripts/ui/sprite_lab.gd	s|UiScale.apply\(get_window\(\), _style\)|pass|	tests/integration/test_sprite_lab_scene.gd	the lab shows a 64px cast in a window built for 16px art
scripts/spritegen/lpc_compose.gd	s|base = parts\[parts.size\(\) - 1\]|pass|	tests/unit/test_lpc_compose.gd	a beast head is remapped from human skin, so its colour silently never changes
scripts/spritegen/lpc_compose.gd	s|if scheme != PALETTE_VERSION:|if false:|	tests/unit/test_lpc_compose.gd	art drawn in a palette scheme nobody fetched is remapped from whatever was loaded
scripts/ui/battle_screen.gd	s|var step := _drawn_width\(\) / STAGGER_WIDTH|var step := 1.0|	tests/integration/test_battle_layout.gd	a party of 64px fighters is stepped as far apart as one of 32px, so they stand in each other
tests/unit/test_imported_art.gd	s|const MAX_GROUND_SPREAD := 2|const MAX_GROUND_SPREAD := 64|	tests/unit/test_imported_art.gd	the ground-line spread is allowed a whole cell, so a cape measured as a foot passes
tools/mutants_scope.sh	s|  grep -qE "\$HARNESS_RE" "\$1"|  true|	tests/unit/test_ci_paths.gd	every change reads as a harness change, so the pull request lane sweeps everything and is the full sweep wearing a fast name
tools/ci_changed.sh	s|  if \[ "\$seen" -eq 0 \]; then needs=true; fi|  :|	tests/unit/test_ci_paths.gd	an empty change list skips the gate, so a base ref that did not resolve reads as a docs change
.github/workflows/ci.yml	s|    if: always\(\)|    if: success()|	tests/unit/test_ci_paths.gd	the required status is skipped when the gate is, and GitHub reports a skipped check as green
tools/map_io.gd	s|\tvar drawn_at := int\(file.data.get\("tile_size", 0\)\)|\tvar drawn_at := 16|	tests/unit/test_map_io.gd	every map is exported on a 16px grid whatever its style is drawn at, so an editor slices the atlas into quarter tiles
scripts/util/tiled_map.gd	s|\tif tile_size > 0 and grid != tile_size:|\tif false:|	tests/unit/test_tiled_map.gd	a map painted on one grid is read on another, and every record on it lands at a fraction of its own tile
scripts/util/ldtk_map.gd	s|\tif tile_size > 0 and grid != tile_size:|\tif false:|	tests/unit/test_ldtk_map.gd	an LDtk project painted on one grid is read on another, and every record moves without the file breaking
scripts/util/tiled_map.gd	s|^\t\t"tilewidth": tile_size,|\t\t"tilewidth": 16,|	tests/unit/test_map_io.gd	an exported map declares a 16px grid whatever it was drawn at, so an editor opens it sliced
scripts/util/ldtk_map.gd	s|"defaultPivotX": 0, "defaultPivotY": 0, "defaultGridSize": tile_size,|"defaultPivotX": 0, "defaultPivotY": 0, "defaultGridSize": 16,|	tests/unit/test_map_io.gd	an exported LDtk project declares a 16px grid whatever it was drawn at
